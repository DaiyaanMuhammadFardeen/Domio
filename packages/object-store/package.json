{
  "name": "@domio/object-store",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Phase 21 — object store abstraction. S3 + MinIO adapters, presigned URLs, factory keyed on env. Pairs with packages/recording-extensions for chunked recording uploads and packages/zip-writer for SCORM packages.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --max-warnings 0",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist .turbo coverage"
  },
  "devDependencies": {
    "@types/node": "22.10.1",
    "typescript": "5.7.2",
    "vitest": "2.1.8"
  }
}
