/**
 * Domio API client — generated.
 *
 * In Phase 0 this is an empty stub. The actual generated code lands in
 * `src/gen/` after `pnpm gen` (which runs `buf generate`). The generated
 * code is committed so consumers don't need Buf installed to use the
 * client.
 *
 * See:
 *   - contracts/proto/domio/v1/*.proto
 *   - contracts/openapi/v1/*.yaml
 *   - docs/adr/0003-contract-first.md
 */

export {};