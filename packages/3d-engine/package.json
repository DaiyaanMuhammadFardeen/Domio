{
  "name": "@domio/3d-engine",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "3D engine module: renderer abstraction (WebGL2/WebGPU), glTF/GLB/USDZ loaders, sanitization, coordinate/color math, env maps, LOD + budget enforcement, scene-editor helpers, exploded views, 3D data viz, particle systems, shader builders, camera keyframes. Phase 11.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --max-warnings 0",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist .turbo coverage"
  },
  "devDependencies": {
    "typescript": "5.7.2",
    "vitest": "2.1.8"
  }
}
