{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://domio.example/contracts/events/collaboration/library.entry_published.json",
  "title": "CollabLibraryEntryPublished",
  "description": "Emitted when a library entry is published (approved and available for use).",
  "type": "object",
  "required": [
    "event_id",
    "event_type",
    "ts_ms",
    "workspace_id",
    "actor_id",
    "actor_type",
    "payload"
  ],
  "properties": {
    "event_id": {
      "type": "string",
      "description": "Globally-unique ULID."
    },
    "event_type": {
      "type": "string",
      "const": "library.entry_published"
    },
    "ts_ms": {
      "type": "integer",
      "format": "int64"
    },
    "workspace_id": {
      "type": "string"
    },
    "deck_id": {
      "type": "string"
    },
    "actor_id": {
      "type": "string"
    },
    "actor_type": {
      "type": "string",
      "enum": ["member", "guest", "agent"]
    },
    "payload": {
      "type": "object",
      "required": ["entry_id", "version_id", "status"],
      "properties": {
        "entry_id": {
          "type": "string"
        },
        "version_id": {
          "type": "string"
        },
        "status": {
          "type": "string",
          "enum": ["draft", "pending", "approved", "retired"]
        }
      }
    }
  }
}
