/**
 * Domio viewer application boundary.
 *
 * Phase 0 intentionally ships this app as a typed placeholder. The public,
 * read-only deck viewer is implemented in the publishing phases.
 */
export const APP_NAME = '@domio/viewer' as const;
