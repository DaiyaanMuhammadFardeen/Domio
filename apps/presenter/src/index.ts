/**
 * Domio presenter application boundary.
 *
 * Phase 0 intentionally ships this app as a typed placeholder. Presenter
 * mode, speaker notes, remote control, and live audience views are added in
 * the presentation delivery phases.
 */
export const APP_NAME = '@domio/presenter' as const;
