{
  "name": "@domio/landing",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Domio marketing site / landing page. Phase 0 stub. Real site lives in the marketing repo or under apps/landing later.",
  "scripts": {
    "build": "next build",
    "dev": "next dev -p 3003",
    "start": "next start -p 3003",
    "lint": "eslint src --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf .next dist .turbo coverage"
  },
  "dependencies": {
    "next": "15.1.3",
    "react": "19.0.0",
    "react-dom": "19.0.0"
  },
  "devDependencies": {
    "@types/node": "22.10.1",
    "@types/react": "19.0.1",
    "@types/react-dom": "19.0.2",
    "typescript": "5.7.2",
    "vitest": "2.1.8"
  },
  "engines": {
    "node": ">=22.11.0"
  }
}