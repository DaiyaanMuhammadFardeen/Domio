/**
 * Domio marketing/landing application boundary.
 *
 * Phase 0 intentionally ships this app as a typed placeholder. The public
 * marketing site is developed independently of the core editor runtime.
 */
export const APP_NAME = '@domio/landing' as const;
