{
  "name": "@domio/test-integration-phase17",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Phase 17 W2 — analytics warehouse integration tests. Verifies ClickHouse materialized-view cardinality/freshness, GraphQL query path, rollup orchestrator idempotency, and JSON Schema round-trip for ingest contracts.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --max-warnings 0"
  },
  "dependencies": {
    "@domio/analytics-warehouse": "workspace:*",
    "@domio/event-ingest": "workspace:*",
    "@domio/observability": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "22.10.1",
    "graphql": "^16.9.0",
    "typescript": "5.7.2",
    "vitest": "2.1.8"
  }
}
