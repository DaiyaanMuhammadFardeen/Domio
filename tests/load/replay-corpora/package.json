{
  "name": "@domio/test-load-replay",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Phase 17 — replay determinism harness + 1M-event corpus fixture. Asserts that running the sessionization consumer 5x produces identical session_id fingerprints.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --max-warnings 0"
  },
  "devDependencies": {
    "@types/node": "22.10.1",
    "typescript": "5.7.2",
    "vitest": "2.1.8"
  }
}
