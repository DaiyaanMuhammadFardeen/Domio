apiVersion: v2
name: secrets
description: |
  Domio secrets — defines the External Secrets Operator resources that
  pull short-lived credentials from Vault into Kubernetes Secrets.
type: application
version: 0.1.0
appVersion: "0.1.0"
keywords:
  - secrets
  - vault
  - external-secrets
home: https://example.com/domio/secrets
sources:
  - https://example.com/domio/secrets
maintainers:
  - name: data-platform
    email: data-platform@example.com
