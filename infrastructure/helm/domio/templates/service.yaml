apiVersion: v1
kind: Service
metadata:
  name: { { include "domio.fullname" . } }
  labels: { { - include "domio.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector: { { - include "domio.selectorLabels" . | nindent 4 } }
