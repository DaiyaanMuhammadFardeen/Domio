apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "domio.fullname" . }}-test-connection"
  labels:
    {{- include "domio.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "domio.fullname" . }}:{{ .Values.service.port }}/healthz']
  restartPolicy: Never