apiVersion: v1
kind: Service
metadata:
  name: { { include "participant-session.fullname" . } }
  labels: { { - include "participant-session.labels" . | nindent 4 } }
spec:
  type: { { .Values.service.type } }
  ports:
    - port: { { .Values.service.port } }
      targetPort: http
      protocol: TCP
      name: http
  selector: { { - include "participant-session.selectorLabels" . | nindent 4 } }
