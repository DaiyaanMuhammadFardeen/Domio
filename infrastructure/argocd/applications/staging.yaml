apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: domio-staging
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: argocd
    app.kubernetes.io/environment: staging
spec:
  project: domio
  source:
    repoURL: https://github.com/domio/infrastructure
    targetRevision: HEAD
    path: infrastructure/helm/domio
    helm:
      releaseName: domio
      valueFiles:
        - values-staging.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: staging
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 5m
