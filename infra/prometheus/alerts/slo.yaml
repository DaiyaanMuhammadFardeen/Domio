# Auto-generated by @domio/obs-control-plane from docs/slos/catalogue.md.
# Do not edit by hand — edit the catalogue and regenerate.

- name: slo-burn-rate-@domio/realtime-gateway-avail-rt-gateway
  rules:
      - alert: SLOBurnHighT1RealtimeGatewayAvailRtGateway1h
        expr: |
          (
            sum(rate(http_requests_total{service="realtime-gateway",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="realtime-gateway"}[1h]))
          ) > 0.014400
        for: 2m
        labels:
          severity: page
          slo: @domio/realtime-gateway-avail-rt-gateway
          tier: tier-1
        annotations:
          summary: "@domio/realtime-gateway avail-rt-gateway burning budget at >14.4× over 1h"
          runbook: "runbooks/realtime-gateway/availability.md"
      - alert: SLOBurnMedT1RealtimeGatewayAvailRtGateway6h
        expr: |
          (
            sum(rate(http_requests_total{service="realtime-gateway",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="realtime-gateway"}[6h]))
          ) > 0.006000
        for: 5m
        labels:
          severity: page
          slo: @domio/realtime-gateway-avail-rt-gateway
          tier: tier-1
        annotations:
          summary: "@domio/realtime-gateway avail-rt-gateway burning budget at >6× over 6h"
          runbook: "runbooks/realtime-gateway/availability.md"
      - alert: SLOBurnLowT1RealtimeGatewayAvailRtGateway24h
        expr: |
          (
            sum(rate(http_requests_total{service="realtime-gateway",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="realtime-gateway"}[24h]))
          ) > 0.003000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/realtime-gateway-avail-rt-gateway
          tier: tier-1
        annotations:
          summary: "@domio/realtime-gateway avail-rt-gateway burning budget at >3× over 24h"
          runbook: "runbooks/realtime-gateway/availability.md"
      - alert: SLOBurnLowT1RealtimeGatewayAvailRtGateway72h
        expr: |
          (
            sum(rate(http_requests_total{service="realtime-gateway",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="realtime-gateway"}[72h]))
          ) > 0.001000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/realtime-gateway-avail-rt-gateway
          tier: tier-1
        annotations:
          summary: "@domio/realtime-gateway avail-rt-gateway burning budget at >1× over 72h"
          runbook: "runbooks/realtime-gateway/availability.md"
- name: slo-burn-rate-@domio/realtime-gateway-lat-rt-gateway-p95
  rules:
      - alert: SLOBurnHighT1RealtimeGatewayLatRtGatewayP951h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="realtime-gateway",le="0.2"}[1h]))
              /
              sum(rate(http_request_duration_seconds_count{service="realtime-gateway"}[1h]))
            )
          ) > 0.720000
        for: 2m
        labels:
          severity: page
          slo: @domio/realtime-gateway-lat-rt-gateway-p95
          tier: tier-1
        annotations:
          summary: "@domio/realtime-gateway lat-rt-gateway-p95 burning budget at >14.4× over 1h"
          runbook: "runbooks/realtime-gateway/latency.md"
      - alert: SLOBurnMedT1RealtimeGatewayLatRtGatewayP956h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="realtime-gateway",le="0.2"}[6h]))
              /
              sum(rate(http_request_duration_seconds_count{service="realtime-gateway"}[6h]))
            )
          ) > 0.300000
        for: 5m
        labels:
          severity: page
          slo: @domio/realtime-gateway-lat-rt-gateway-p95
          tier: tier-1
        annotations:
          summary: "@domio/realtime-gateway lat-rt-gateway-p95 burning budget at >6× over 6h"
          runbook: "runbooks/realtime-gateway/latency.md"
      - alert: SLOBurnLowT1RealtimeGatewayLatRtGatewayP9524h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="realtime-gateway",le="0.2"}[24h]))
              /
              sum(rate(http_request_duration_seconds_count{service="realtime-gateway"}[24h]))
            )
          ) > 0.150000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/realtime-gateway-lat-rt-gateway-p95
          tier: tier-1
        annotations:
          summary: "@domio/realtime-gateway lat-rt-gateway-p95 burning budget at >3× over 24h"
          runbook: "runbooks/realtime-gateway/latency.md"
      - alert: SLOBurnLowT1RealtimeGatewayLatRtGatewayP9572h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="realtime-gateway",le="0.2"}[72h]))
              /
              sum(rate(http_request_duration_seconds_count{service="realtime-gateway"}[72h]))
            )
          ) > 0.050000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/realtime-gateway-lat-rt-gateway-p95
          tier: tier-1
        annotations:
          summary: "@domio/realtime-gateway lat-rt-gateway-p95 burning budget at >1× over 72h"
          runbook: "runbooks/realtime-gateway/latency.md"
- name: slo-burn-rate-@domio/participant-ws-gateway-avail-participant-ws
  rules:
      - alert: SLOBurnHighT1ParticipantWsGatewayAvailParticipantWs1h
        expr: |
          (
            sum(rate(http_requests_total{service="participant-ws-gateway",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="participant-ws-gateway"}[1h]))
          ) > 0.014400
        for: 2m
        labels:
          severity: page
          slo: @domio/participant-ws-gateway-avail-participant-ws
          tier: tier-1
        annotations:
          summary: "@domio/participant-ws-gateway avail-participant-ws burning budget at >14.4× over 1h"
          runbook: "runbooks/participant-ws-gateway/availability.md"
      - alert: SLOBurnMedT1ParticipantWsGatewayAvailParticipantWs6h
        expr: |
          (
            sum(rate(http_requests_total{service="participant-ws-gateway",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="participant-ws-gateway"}[6h]))
          ) > 0.006000
        for: 5m
        labels:
          severity: page
          slo: @domio/participant-ws-gateway-avail-participant-ws
          tier: tier-1
        annotations:
          summary: "@domio/participant-ws-gateway avail-participant-ws burning budget at >6× over 6h"
          runbook: "runbooks/participant-ws-gateway/availability.md"
      - alert: SLOBurnLowT1ParticipantWsGatewayAvailParticipantWs24h
        expr: |
          (
            sum(rate(http_requests_total{service="participant-ws-gateway",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="participant-ws-gateway"}[24h]))
          ) > 0.003000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/participant-ws-gateway-avail-participant-ws
          tier: tier-1
        annotations:
          summary: "@domio/participant-ws-gateway avail-participant-ws burning budget at >3× over 24h"
          runbook: "runbooks/participant-ws-gateway/availability.md"
      - alert: SLOBurnLowT1ParticipantWsGatewayAvailParticipantWs72h
        expr: |
          (
            sum(rate(http_requests_total{service="participant-ws-gateway",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="participant-ws-gateway"}[72h]))
          ) > 0.001000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/participant-ws-gateway-avail-participant-ws
          tier: tier-1
        annotations:
          summary: "@domio/participant-ws-gateway avail-participant-ws burning budget at >1× over 72h"
          runbook: "runbooks/participant-ws-gateway/availability.md"
- name: slo-burn-rate-@domio/audience-service-avail-audience
  rules:
      - alert: SLOBurnHighT1AudienceAvailAudience1h
        expr: |
          (
            sum(rate(http_requests_total{service="audience",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="audience"}[1h]))
          ) > 0.014400
        for: 2m
        labels:
          severity: page
          slo: @domio/audience-service-avail-audience
          tier: tier-1
        annotations:
          summary: "@domio/audience-service avail-audience burning budget at >14.4× over 1h"
          runbook: "runbooks/audience-service/availability.md"
      - alert: SLOBurnMedT1AudienceAvailAudience6h
        expr: |
          (
            sum(rate(http_requests_total{service="audience",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="audience"}[6h]))
          ) > 0.006000
        for: 5m
        labels:
          severity: page
          slo: @domio/audience-service-avail-audience
          tier: tier-1
        annotations:
          summary: "@domio/audience-service avail-audience burning budget at >6× over 6h"
          runbook: "runbooks/audience-service/availability.md"
      - alert: SLOBurnLowT1AudienceAvailAudience24h
        expr: |
          (
            sum(rate(http_requests_total{service="audience",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="audience"}[24h]))
          ) > 0.003000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/audience-service-avail-audience
          tier: tier-1
        annotations:
          summary: "@domio/audience-service avail-audience burning budget at >3× over 24h"
          runbook: "runbooks/audience-service/availability.md"
      - alert: SLOBurnLowT1AudienceAvailAudience72h
        expr: |
          (
            sum(rate(http_requests_total{service="audience",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="audience"}[72h]))
          ) > 0.001000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/audience-service-avail-audience
          tier: tier-1
        annotations:
          summary: "@domio/audience-service avail-audience burning budget at >1× over 72h"
          runbook: "runbooks/audience-service/availability.md"
- name: slo-burn-rate-@domio/audience-service-lat-audience-render-p95
  rules:
      - alert: SLOBurnHighT1AudienceLatAudienceRenderP951h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="audience",le="0.25"}[1h]))
              /
              sum(rate(http_request_duration_seconds_count{service="audience"}[1h]))
            )
          ) > 0.720000
        for: 2m
        labels:
          severity: page
          slo: @domio/audience-service-lat-audience-render-p95
          tier: tier-1
        annotations:
          summary: "@domio/audience-service lat-audience-render-p95 burning budget at >14.4× over 1h"
          runbook: "runbooks/audience-service/latency.md"
      - alert: SLOBurnMedT1AudienceLatAudienceRenderP956h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="audience",le="0.25"}[6h]))
              /
              sum(rate(http_request_duration_seconds_count{service="audience"}[6h]))
            )
          ) > 0.300000
        for: 5m
        labels:
          severity: page
          slo: @domio/audience-service-lat-audience-render-p95
          tier: tier-1
        annotations:
          summary: "@domio/audience-service lat-audience-render-p95 burning budget at >6× over 6h"
          runbook: "runbooks/audience-service/latency.md"
      - alert: SLOBurnLowT1AudienceLatAudienceRenderP9524h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="audience",le="0.25"}[24h]))
              /
              sum(rate(http_request_duration_seconds_count{service="audience"}[24h]))
            )
          ) > 0.150000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/audience-service-lat-audience-render-p95
          tier: tier-1
        annotations:
          summary: "@domio/audience-service lat-audience-render-p95 burning budget at >3× over 24h"
          runbook: "runbooks/audience-service/latency.md"
      - alert: SLOBurnLowT1AudienceLatAudienceRenderP9572h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="audience",le="0.25"}[72h]))
              /
              sum(rate(http_request_duration_seconds_count{service="audience"}[72h]))
            )
          ) > 0.050000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/audience-service-lat-audience-render-p95
          tier: tier-1
        annotations:
          summary: "@domio/audience-service lat-audience-render-p95 burning budget at >1× over 72h"
          runbook: "runbooks/audience-service/latency.md"
- name: slo-burn-rate-@domio/presenter-session-avail-presenter-session
  rules:
      - alert: SLOBurnHighT1PresenterSessionAvailPresenterSession1h
        expr: |
          (
            sum(rate(http_requests_total{service="presenter-session",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="presenter-session"}[1h]))
          ) > 0.014400
        for: 2m
        labels:
          severity: page
          slo: @domio/presenter-session-avail-presenter-session
          tier: tier-1
        annotations:
          summary: "@domio/presenter-session avail-presenter-session burning budget at >14.4× over 1h"
          runbook: "runbooks/presenter-session/availability.md"
      - alert: SLOBurnMedT1PresenterSessionAvailPresenterSession6h
        expr: |
          (
            sum(rate(http_requests_total{service="presenter-session",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="presenter-session"}[6h]))
          ) > 0.006000
        for: 5m
        labels:
          severity: page
          slo: @domio/presenter-session-avail-presenter-session
          tier: tier-1
        annotations:
          summary: "@domio/presenter-session avail-presenter-session burning budget at >6× over 6h"
          runbook: "runbooks/presenter-session/availability.md"
      - alert: SLOBurnLowT1PresenterSessionAvailPresenterSession24h
        expr: |
          (
            sum(rate(http_requests_total{service="presenter-session",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="presenter-session"}[24h]))
          ) > 0.003000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/presenter-session-avail-presenter-session
          tier: tier-1
        annotations:
          summary: "@domio/presenter-session avail-presenter-session burning budget at >3× over 24h"
          runbook: "runbooks/presenter-session/availability.md"
      - alert: SLOBurnLowT1PresenterSessionAvailPresenterSession72h
        expr: |
          (
            sum(rate(http_requests_total{service="presenter-session",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="presenter-session"}[72h]))
          ) > 0.001000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/presenter-session-avail-presenter-session
          tier: tier-1
        annotations:
          summary: "@domio/presenter-session avail-presenter-session burning budget at >1× over 72h"
          runbook: "runbooks/presenter-session/availability.md"
- name: slo-burn-rate-@domio/presenter-session-lat-presenter-action-p95
  rules:
      - alert: SLOBurnHighT1PresenterSessionLatPresenterActionP951h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="presenter-session",le="0.15"}[1h]))
              /
              sum(rate(http_request_duration_seconds_count{service="presenter-session"}[1h]))
            )
          ) > 0.720000
        for: 2m
        labels:
          severity: page
          slo: @domio/presenter-session-lat-presenter-action-p95
          tier: tier-1
        annotations:
          summary: "@domio/presenter-session lat-presenter-action-p95 burning budget at >14.4× over 1h"
          runbook: "runbooks/presenter-session/latency.md"
      - alert: SLOBurnMedT1PresenterSessionLatPresenterActionP956h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="presenter-session",le="0.15"}[6h]))
              /
              sum(rate(http_request_duration_seconds_count{service="presenter-session"}[6h]))
            )
          ) > 0.300000
        for: 5m
        labels:
          severity: page
          slo: @domio/presenter-session-lat-presenter-action-p95
          tier: tier-1
        annotations:
          summary: "@domio/presenter-session lat-presenter-action-p95 burning budget at >6× over 6h"
          runbook: "runbooks/presenter-session/latency.md"
      - alert: SLOBurnLowT1PresenterSessionLatPresenterActionP9524h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="presenter-session",le="0.15"}[24h]))
              /
              sum(rate(http_request_duration_seconds_count{service="presenter-session"}[24h]))
            )
          ) > 0.150000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/presenter-session-lat-presenter-action-p95
          tier: tier-1
        annotations:
          summary: "@domio/presenter-session lat-presenter-action-p95 burning budget at >3× over 24h"
          runbook: "runbooks/presenter-session/latency.md"
      - alert: SLOBurnLowT1PresenterSessionLatPresenterActionP9572h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="presenter-session",le="0.15"}[72h]))
              /
              sum(rate(http_request_duration_seconds_count{service="presenter-session"}[72h]))
            )
          ) > 0.050000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/presenter-session-lat-presenter-action-p95
          tier: tier-1
        annotations:
          summary: "@domio/presenter-session lat-presenter-action-p95 burning budget at >1× over 72h"
          runbook: "runbooks/presenter-session/latency.md"
- name: slo-burn-rate-@domio/share-api-avail-share-api
  rules:
      - alert: SLOBurnHighT1ShareApiAvailShareApi1h
        expr: |
          (
            sum(rate(http_requests_total{service="share-api",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="share-api"}[1h]))
          ) > 0.014400
        for: 2m
        labels:
          severity: page
          slo: @domio/share-api-avail-share-api
          tier: tier-1
        annotations:
          summary: "@domio/share-api avail-share-api burning budget at >14.4× over 1h"
          runbook: "runbooks/share-api/availability.md"
      - alert: SLOBurnMedT1ShareApiAvailShareApi6h
        expr: |
          (
            sum(rate(http_requests_total{service="share-api",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="share-api"}[6h]))
          ) > 0.006000
        for: 5m
        labels:
          severity: page
          slo: @domio/share-api-avail-share-api
          tier: tier-1
        annotations:
          summary: "@domio/share-api avail-share-api burning budget at >6× over 6h"
          runbook: "runbooks/share-api/availability.md"
      - alert: SLOBurnLowT1ShareApiAvailShareApi24h
        expr: |
          (
            sum(rate(http_requests_total{service="share-api",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="share-api"}[24h]))
          ) > 0.003000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/share-api-avail-share-api
          tier: tier-1
        annotations:
          summary: "@domio/share-api avail-share-api burning budget at >3× over 24h"
          runbook: "runbooks/share-api/availability.md"
      - alert: SLOBurnLowT1ShareApiAvailShareApi72h
        expr: |
          (
            sum(rate(http_requests_total{service="share-api",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="share-api"}[72h]))
          ) > 0.001000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/share-api-avail-share-api
          tier: tier-1
        annotations:
          summary: "@domio/share-api avail-share-api burning budget at >1× over 72h"
          runbook: "runbooks/share-api/availability.md"
- name: slo-burn-rate-@domio/publish-service-avail-publish
  rules:
      - alert: SLOBurnHighT2PublishAvailPublish1h
        expr: |
          (
            sum(rate(http_requests_total{service="publish",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="publish"}[1h]))
          ) > 0.072000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/publish-service-avail-publish
          tier: tier-2
        annotations:
          summary: "@domio/publish-service avail-publish burning budget at >14.4× over 1h"
          runbook: "runbooks/publish-service/availability.md"
      - alert: SLOBurnMedT2PublishAvailPublish6h
        expr: |
          (
            sum(rate(http_requests_total{service="publish",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="publish"}[6h]))
          ) > 0.030000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/publish-service-avail-publish
          tier: tier-2
        annotations:
          summary: "@domio/publish-service avail-publish burning budget at >6× over 6h"
          runbook: "runbooks/publish-service/availability.md"
      - alert: SLOBurnLowT2PublishAvailPublish24h
        expr: |
          (
            sum(rate(http_requests_total{service="publish",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="publish"}[24h]))
          ) > 0.015000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/publish-service-avail-publish
          tier: tier-2
        annotations:
          summary: "@domio/publish-service avail-publish burning budget at >3× over 24h"
          runbook: "runbooks/publish-service/availability.md"
      - alert: SLOBurnLowT2PublishAvailPublish72h
        expr: |
          (
            sum(rate(http_requests_total{service="publish",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="publish"}[72h]))
          ) > 0.005000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/publish-service-avail-publish
          tier: tier-2
        annotations:
          summary: "@domio/publish-service avail-publish burning budget at >1× over 72h"
          runbook: "runbooks/publish-service/availability.md"
- name: slo-burn-rate-@domio/audit-service-avail-audit
  rules:
      - alert: SLOBurnHighT1AuditAvailAudit1h
        expr: |
          (
            sum(rate(http_requests_total{service="audit",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="audit"}[1h]))
          ) > 0.014400
        for: 2m
        labels:
          severity: page
          slo: @domio/audit-service-avail-audit
          tier: tier-1
        annotations:
          summary: "@domio/audit-service avail-audit burning budget at >14.4× over 1h"
          runbook: "runbooks/audit-service/availability.md"
      - alert: SLOBurnMedT1AuditAvailAudit6h
        expr: |
          (
            sum(rate(http_requests_total{service="audit",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="audit"}[6h]))
          ) > 0.006000
        for: 5m
        labels:
          severity: page
          slo: @domio/audit-service-avail-audit
          tier: tier-1
        annotations:
          summary: "@domio/audit-service avail-audit burning budget at >6× over 6h"
          runbook: "runbooks/audit-service/availability.md"
      - alert: SLOBurnLowT1AuditAvailAudit24h
        expr: |
          (
            sum(rate(http_requests_total{service="audit",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="audit"}[24h]))
          ) > 0.003000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/audit-service-avail-audit
          tier: tier-1
        annotations:
          summary: "@domio/audit-service avail-audit burning budget at >3× over 24h"
          runbook: "runbooks/audit-service/availability.md"
      - alert: SLOBurnLowT1AuditAvailAudit72h
        expr: |
          (
            sum(rate(http_requests_total{service="audit",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="audit"}[72h]))
          ) > 0.001000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/audit-service-avail-audit
          tier: tier-1
        annotations:
          summary: "@domio/audit-service avail-audit burning budget at >1× over 72h"
          runbook: "runbooks/audit-service/availability.md"
- name: slo-burn-rate-@domio/permission-engine-avail-permission-engine
  rules:
      - alert: SLOBurnHighT1PermissionEngineAvailPermissionEngine1h
        expr: |
          (
            sum(rate(http_requests_total{service="permission-engine",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="permission-engine"}[1h]))
          ) > 0.014400
        for: 2m
        labels:
          severity: page
          slo: @domio/permission-engine-avail-permission-engine
          tier: tier-1
        annotations:
          summary: "@domio/permission-engine avail-permission-engine burning budget at >14.4× over 1h"
          runbook: "runbooks/permission-engine/availability.md"
      - alert: SLOBurnMedT1PermissionEngineAvailPermissionEngine6h
        expr: |
          (
            sum(rate(http_requests_total{service="permission-engine",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="permission-engine"}[6h]))
          ) > 0.006000
        for: 5m
        labels:
          severity: page
          slo: @domio/permission-engine-avail-permission-engine
          tier: tier-1
        annotations:
          summary: "@domio/permission-engine avail-permission-engine burning budget at >6× over 6h"
          runbook: "runbooks/permission-engine/availability.md"
      - alert: SLOBurnLowT1PermissionEngineAvailPermissionEngine24h
        expr: |
          (
            sum(rate(http_requests_total{service="permission-engine",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="permission-engine"}[24h]))
          ) > 0.003000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/permission-engine-avail-permission-engine
          tier: tier-1
        annotations:
          summary: "@domio/permission-engine avail-permission-engine burning budget at >3× over 24h"
          runbook: "runbooks/permission-engine/availability.md"
      - alert: SLOBurnLowT1PermissionEngineAvailPermissionEngine72h
        expr: |
          (
            sum(rate(http_requests_total{service="permission-engine",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="permission-engine"}[72h]))
          ) > 0.001000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/permission-engine-avail-permission-engine
          tier: tier-1
        annotations:
          summary: "@domio/permission-engine avail-permission-engine burning budget at >1× over 72h"
          runbook: "runbooks/permission-engine/availability.md"
- name: slo-burn-rate-@domio/permission-engine-lat-permission-engine-p95
  rules:
      - alert: SLOBurnHighT1PermissionEngineLatPermissionEngineP951h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="permission-engine",le="0.005"}[1h]))
              /
              sum(rate(http_request_duration_seconds_count{service="permission-engine"}[1h]))
            )
          ) > 0.720000
        for: 2m
        labels:
          severity: page
          slo: @domio/permission-engine-lat-permission-engine-p95
          tier: tier-1
        annotations:
          summary: "@domio/permission-engine lat-permission-engine-p95 burning budget at >14.4× over 1h"
          runbook: "runbooks/permission-engine/latency.md"
      - alert: SLOBurnMedT1PermissionEngineLatPermissionEngineP956h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="permission-engine",le="0.005"}[6h]))
              /
              sum(rate(http_request_duration_seconds_count{service="permission-engine"}[6h]))
            )
          ) > 0.300000
        for: 5m
        labels:
          severity: page
          slo: @domio/permission-engine-lat-permission-engine-p95
          tier: tier-1
        annotations:
          summary: "@domio/permission-engine lat-permission-engine-p95 burning budget at >6× over 6h"
          runbook: "runbooks/permission-engine/latency.md"
      - alert: SLOBurnLowT1PermissionEngineLatPermissionEngineP9524h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="permission-engine",le="0.005"}[24h]))
              /
              sum(rate(http_request_duration_seconds_count{service="permission-engine"}[24h]))
            )
          ) > 0.150000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/permission-engine-lat-permission-engine-p95
          tier: tier-1
        annotations:
          summary: "@domio/permission-engine lat-permission-engine-p95 burning budget at >3× over 24h"
          runbook: "runbooks/permission-engine/latency.md"
      - alert: SLOBurnLowT1PermissionEngineLatPermissionEngineP9572h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="permission-engine",le="0.005"}[72h]))
              /
              sum(rate(http_request_duration_seconds_count{service="permission-engine"}[72h]))
            )
          ) > 0.050000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/permission-engine-lat-permission-engine-p95
          tier: tier-1
        annotations:
          summary: "@domio/permission-engine lat-permission-engine-p95 burning budget at >1× over 72h"
          runbook: "runbooks/permission-engine/latency.md"
- name: slo-burn-rate-@domio/auth-avail-auth
  rules:
      - alert: SLOBurnHighT1AuthAvailAuth1h
        expr: |
          (
            sum(rate(http_requests_total{service="auth",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="auth"}[1h]))
          ) > 0.014400
        for: 2m
        labels:
          severity: page
          slo: @domio/auth-avail-auth
          tier: tier-1
        annotations:
          summary: "@domio/auth avail-auth burning budget at >14.4× over 1h"
          runbook: "runbooks/auth/availability.md"
      - alert: SLOBurnMedT1AuthAvailAuth6h
        expr: |
          (
            sum(rate(http_requests_total{service="auth",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="auth"}[6h]))
          ) > 0.006000
        for: 5m
        labels:
          severity: page
          slo: @domio/auth-avail-auth
          tier: tier-1
        annotations:
          summary: "@domio/auth avail-auth burning budget at >6× over 6h"
          runbook: "runbooks/auth/availability.md"
      - alert: SLOBurnLowT1AuthAvailAuth24h
        expr: |
          (
            sum(rate(http_requests_total{service="auth",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="auth"}[24h]))
          ) > 0.003000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/auth-avail-auth
          tier: tier-1
        annotations:
          summary: "@domio/auth avail-auth burning budget at >3× over 24h"
          runbook: "runbooks/auth/availability.md"
      - alert: SLOBurnLowT1AuthAvailAuth72h
        expr: |
          (
            sum(rate(http_requests_total{service="auth",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="auth"}[72h]))
          ) > 0.001000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/auth-avail-auth
          tier: tier-1
        annotations:
          summary: "@domio/auth avail-auth burning budget at >1× over 72h"
          runbook: "runbooks/auth/availability.md"
- name: slo-burn-rate-@domio/auth-lat-auth-p95
  rules:
      - alert: SLOBurnHighT1AuthLatAuthP951h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="auth",le="0.3"}[1h]))
              /
              sum(rate(http_request_duration_seconds_count{service="auth"}[1h]))
            )
          ) > 0.720000
        for: 2m
        labels:
          severity: page
          slo: @domio/auth-lat-auth-p95
          tier: tier-1
        annotations:
          summary: "@domio/auth lat-auth-p95 burning budget at >14.4× over 1h"
          runbook: "runbooks/auth/latency.md"
      - alert: SLOBurnMedT1AuthLatAuthP956h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="auth",le="0.3"}[6h]))
              /
              sum(rate(http_request_duration_seconds_count{service="auth"}[6h]))
            )
          ) > 0.300000
        for: 5m
        labels:
          severity: page
          slo: @domio/auth-lat-auth-p95
          tier: tier-1
        annotations:
          summary: "@domio/auth lat-auth-p95 burning budget at >6× over 6h"
          runbook: "runbooks/auth/latency.md"
      - alert: SLOBurnLowT1AuthLatAuthP9524h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="auth",le="0.3"}[24h]))
              /
              sum(rate(http_request_duration_seconds_count{service="auth"}[24h]))
            )
          ) > 0.150000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/auth-lat-auth-p95
          tier: tier-1
        annotations:
          summary: "@domio/auth lat-auth-p95 burning budget at >3× over 24h"
          runbook: "runbooks/auth/latency.md"
      - alert: SLOBurnLowT1AuthLatAuthP9572h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="auth",le="0.3"}[72h]))
              /
              sum(rate(http_request_duration_seconds_count{service="auth"}[72h]))
            )
          ) > 0.050000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/auth-lat-auth-p95
          tier: tier-1
        annotations:
          summary: "@domio/auth lat-auth-p95 burning budget at >1× over 72h"
          runbook: "runbooks/auth/latency.md"
- name: slo-burn-rate-@domio/billing-avail-billing
  rules:
      - alert: SLOBurnHighT1BillingAvailBilling1h
        expr: |
          (
            sum(rate(http_requests_total{service="billing",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="billing"}[1h]))
          ) > 0.014400
        for: 2m
        labels:
          severity: page
          slo: @domio/billing-avail-billing
          tier: tier-1
        annotations:
          summary: "@domio/billing avail-billing burning budget at >14.4× over 1h"
          runbook: "runbooks/billing/availability.md"
      - alert: SLOBurnMedT1BillingAvailBilling6h
        expr: |
          (
            sum(rate(http_requests_total{service="billing",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="billing"}[6h]))
          ) > 0.006000
        for: 5m
        labels:
          severity: page
          slo: @domio/billing-avail-billing
          tier: tier-1
        annotations:
          summary: "@domio/billing avail-billing burning budget at >6× over 6h"
          runbook: "runbooks/billing/availability.md"
      - alert: SLOBurnLowT1BillingAvailBilling24h
        expr: |
          (
            sum(rate(http_requests_total{service="billing",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="billing"}[24h]))
          ) > 0.003000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/billing-avail-billing
          tier: tier-1
        annotations:
          summary: "@domio/billing avail-billing burning budget at >3× over 24h"
          runbook: "runbooks/billing/availability.md"
      - alert: SLOBurnLowT1BillingAvailBilling72h
        expr: |
          (
            sum(rate(http_requests_total{service="billing",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="billing"}[72h]))
          ) > 0.001000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/billing-avail-billing
          tier: tier-1
        annotations:
          summary: "@domio/billing avail-billing burning budget at >1× over 72h"
          runbook: "runbooks/billing/availability.md"
- name: slo-burn-rate-@domio/ai-orchestrator-avail-ai-orchestrator
  rules:
      - alert: SLOBurnHighT2AiOrchestratorAvailAiOrchestrator1h
        expr: |
          (
            sum(rate(http_requests_total{service="ai-orchestrator",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="ai-orchestrator"}[1h]))
          ) > 0.072000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/ai-orchestrator-avail-ai-orchestrator
          tier: tier-2
        annotations:
          summary: "@domio/ai-orchestrator avail-ai-orchestrator burning budget at >14.4× over 1h"
          runbook: "runbooks/ai-orchestrator/availability.md"
      - alert: SLOBurnMedT2AiOrchestratorAvailAiOrchestrator6h
        expr: |
          (
            sum(rate(http_requests_total{service="ai-orchestrator",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="ai-orchestrator"}[6h]))
          ) > 0.030000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/ai-orchestrator-avail-ai-orchestrator
          tier: tier-2
        annotations:
          summary: "@domio/ai-orchestrator avail-ai-orchestrator burning budget at >6× over 6h"
          runbook: "runbooks/ai-orchestrator/availability.md"
      - alert: SLOBurnLowT2AiOrchestratorAvailAiOrchestrator24h
        expr: |
          (
            sum(rate(http_requests_total{service="ai-orchestrator",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="ai-orchestrator"}[24h]))
          ) > 0.015000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/ai-orchestrator-avail-ai-orchestrator
          tier: tier-2
        annotations:
          summary: "@domio/ai-orchestrator avail-ai-orchestrator burning budget at >3× over 24h"
          runbook: "runbooks/ai-orchestrator/availability.md"
      - alert: SLOBurnLowT2AiOrchestratorAvailAiOrchestrator72h
        expr: |
          (
            sum(rate(http_requests_total{service="ai-orchestrator",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="ai-orchestrator"}[72h]))
          ) > 0.005000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/ai-orchestrator-avail-ai-orchestrator
          tier: tier-2
        annotations:
          summary: "@domio/ai-orchestrator avail-ai-orchestrator burning budget at >1× over 72h"
          runbook: "runbooks/ai-orchestrator/availability.md"
- name: slo-burn-rate-@domio/ai-adapters-lat-ai-adapter-p95
  rules:
      - alert: SLOBurnHighT2AiAdaptersLatAiAdapterP951h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="ai-adapters",le="3"}[1h]))
              /
              sum(rate(http_request_duration_seconds_count{service="ai-adapters"}[1h]))
            )
          ) > 0.720000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/ai-adapters-lat-ai-adapter-p95
          tier: tier-2
        annotations:
          summary: "@domio/ai-adapters lat-ai-adapter-p95 burning budget at >14.4× over 1h"
          runbook: "runbooks/ai-adapters/latency.md"
      - alert: SLOBurnMedT2AiAdaptersLatAiAdapterP956h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="ai-adapters",le="3"}[6h]))
              /
              sum(rate(http_request_duration_seconds_count{service="ai-adapters"}[6h]))
            )
          ) > 0.300000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/ai-adapters-lat-ai-adapter-p95
          tier: tier-2
        annotations:
          summary: "@domio/ai-adapters lat-ai-adapter-p95 burning budget at >6× over 6h"
          runbook: "runbooks/ai-adapters/latency.md"
      - alert: SLOBurnLowT2AiAdaptersLatAiAdapterP9524h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="ai-adapters",le="3"}[24h]))
              /
              sum(rate(http_request_duration_seconds_count{service="ai-adapters"}[24h]))
            )
          ) > 0.150000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/ai-adapters-lat-ai-adapter-p95
          tier: tier-2
        annotations:
          summary: "@domio/ai-adapters lat-ai-adapter-p95 burning budget at >3× over 24h"
          runbook: "runbooks/ai-adapters/latency.md"
      - alert: SLOBurnLowT2AiAdaptersLatAiAdapterP9572h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="ai-adapters",le="3"}[72h]))
              /
              sum(rate(http_request_duration_seconds_count{service="ai-adapters"}[72h]))
            )
          ) > 0.050000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/ai-adapters-lat-ai-adapter-p95
          tier: tier-2
        annotations:
          summary: "@domio/ai-adapters lat-ai-adapter-p95 burning budget at >1× over 72h"
          runbook: "runbooks/ai-adapters/latency.md"
- name: slo-burn-rate-@domio/mcp-server-service-avail-mcp-server
  rules:
      - alert: SLOBurnHighT2McpServerAvailMcpServer1h
        expr: |
          (
            sum(rate(http_requests_total{service="mcp-server",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="mcp-server"}[1h]))
          ) > 0.072000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/mcp-server-service-avail-mcp-server
          tier: tier-2
        annotations:
          summary: "@domio/mcp-server-service avail-mcp-server burning budget at >14.4× over 1h"
          runbook: "runbooks/mcp-server-service/availability.md"
      - alert: SLOBurnMedT2McpServerAvailMcpServer6h
        expr: |
          (
            sum(rate(http_requests_total{service="mcp-server",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="mcp-server"}[6h]))
          ) > 0.030000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/mcp-server-service-avail-mcp-server
          tier: tier-2
        annotations:
          summary: "@domio/mcp-server-service avail-mcp-server burning budget at >6× over 6h"
          runbook: "runbooks/mcp-server-service/availability.md"
      - alert: SLOBurnLowT2McpServerAvailMcpServer24h
        expr: |
          (
            sum(rate(http_requests_total{service="mcp-server",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="mcp-server"}[24h]))
          ) > 0.015000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/mcp-server-service-avail-mcp-server
          tier: tier-2
        annotations:
          summary: "@domio/mcp-server-service avail-mcp-server burning budget at >3× over 24h"
          runbook: "runbooks/mcp-server-service/availability.md"
      - alert: SLOBurnLowT2McpServerAvailMcpServer72h
        expr: |
          (
            sum(rate(http_requests_total{service="mcp-server",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="mcp-server"}[72h]))
          ) > 0.005000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/mcp-server-service-avail-mcp-server
          tier: tier-2
        annotations:
          summary: "@domio/mcp-server-service avail-mcp-server burning budget at >1× over 72h"
          runbook: "runbooks/mcp-server-service/availability.md"
- name: slo-burn-rate-@domio/control-plane-avail-control-plane
  rules:
      - alert: SLOBurnHighT2ControlPlaneAvailControlPlane1h
        expr: |
          (
            sum(rate(http_requests_total{service="control-plane",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="control-plane"}[1h]))
          ) > 0.072000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/control-plane-avail-control-plane
          tier: tier-2
        annotations:
          summary: "@domio/control-plane avail-control-plane burning budget at >14.4× over 1h"
          runbook: "runbooks/control-plane/availability.md"
      - alert: SLOBurnMedT2ControlPlaneAvailControlPlane6h
        expr: |
          (
            sum(rate(http_requests_total{service="control-plane",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="control-plane"}[6h]))
          ) > 0.030000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/control-plane-avail-control-plane
          tier: tier-2
        annotations:
          summary: "@domio/control-plane avail-control-plane burning budget at >6× over 6h"
          runbook: "runbooks/control-plane/availability.md"
      - alert: SLOBurnLowT2ControlPlaneAvailControlPlane24h
        expr: |
          (
            sum(rate(http_requests_total{service="control-plane",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="control-plane"}[24h]))
          ) > 0.015000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/control-plane-avail-control-plane
          tier: tier-2
        annotations:
          summary: "@domio/control-plane avail-control-plane burning budget at >3× over 24h"
          runbook: "runbooks/control-plane/availability.md"
      - alert: SLOBurnLowT2ControlPlaneAvailControlPlane72h
        expr: |
          (
            sum(rate(http_requests_total{service="control-plane",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="control-plane"}[72h]))
          ) > 0.005000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/control-plane-avail-control-plane
          tier: tier-2
        annotations:
          summary: "@domio/control-plane avail-control-plane burning budget at >1× over 72h"
          runbook: "runbooks/control-plane/availability.md"
- name: slo-burn-rate-@domio/event-ingest-lat-event-ingest-p95
  rules:
      - alert: SLOBurnHighT2EventIngestLatEventIngestP951h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="event-ingest",le="0.1"}[1h]))
              /
              sum(rate(http_request_duration_seconds_count{service="event-ingest"}[1h]))
            )
          ) > 0.720000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/event-ingest-lat-event-ingest-p95
          tier: tier-2
        annotations:
          summary: "@domio/event-ingest lat-event-ingest-p95 burning budget at >14.4× over 1h"
          runbook: "runbooks/event-ingest/latency.md"
      - alert: SLOBurnMedT2EventIngestLatEventIngestP956h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="event-ingest",le="0.1"}[6h]))
              /
              sum(rate(http_request_duration_seconds_count{service="event-ingest"}[6h]))
            )
          ) > 0.300000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/event-ingest-lat-event-ingest-p95
          tier: tier-2
        annotations:
          summary: "@domio/event-ingest lat-event-ingest-p95 burning budget at >6× over 6h"
          runbook: "runbooks/event-ingest/latency.md"
      - alert: SLOBurnLowT2EventIngestLatEventIngestP9524h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="event-ingest",le="0.1"}[24h]))
              /
              sum(rate(http_request_duration_seconds_count{service="event-ingest"}[24h]))
            )
          ) > 0.150000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/event-ingest-lat-event-ingest-p95
          tier: tier-2
        annotations:
          summary: "@domio/event-ingest lat-event-ingest-p95 burning budget at >3× over 24h"
          runbook: "runbooks/event-ingest/latency.md"
      - alert: SLOBurnLowT2EventIngestLatEventIngestP9572h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="event-ingest",le="0.1"}[72h]))
              /
              sum(rate(http_request_duration_seconds_count{service="event-ingest"}[72h]))
            )
          ) > 0.050000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/event-ingest-lat-event-ingest-p95
          tier: tier-2
        annotations:
          summary: "@domio/event-ingest lat-event-ingest-p95 burning budget at >1× over 72h"
          runbook: "runbooks/event-ingest/latency.md"
- name: slo-burn-rate-@domio/clickhouse-loader-avail-clickhouse-loader
  rules:
      - alert: SLOBurnHighT2ClickhouseLoaderAvailClickhouseLoader1h
        expr: |
          (
            sum(rate(http_requests_total{service="clickhouse-loader",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="clickhouse-loader"}[1h]))
          ) > 0.072000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/clickhouse-loader-avail-clickhouse-loader
          tier: tier-2
        annotations:
          summary: "@domio/clickhouse-loader avail-clickhouse-loader burning budget at >14.4× over 1h"
          runbook: "runbooks/clickhouse-loader/availability.md"
      - alert: SLOBurnMedT2ClickhouseLoaderAvailClickhouseLoader6h
        expr: |
          (
            sum(rate(http_requests_total{service="clickhouse-loader",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="clickhouse-loader"}[6h]))
          ) > 0.030000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/clickhouse-loader-avail-clickhouse-loader
          tier: tier-2
        annotations:
          summary: "@domio/clickhouse-loader avail-clickhouse-loader burning budget at >6× over 6h"
          runbook: "runbooks/clickhouse-loader/availability.md"
      - alert: SLOBurnLowT2ClickhouseLoaderAvailClickhouseLoader24h
        expr: |
          (
            sum(rate(http_requests_total{service="clickhouse-loader",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="clickhouse-loader"}[24h]))
          ) > 0.015000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/clickhouse-loader-avail-clickhouse-loader
          tier: tier-2
        annotations:
          summary: "@domio/clickhouse-loader avail-clickhouse-loader burning budget at >3× over 24h"
          runbook: "runbooks/clickhouse-loader/availability.md"
      - alert: SLOBurnLowT2ClickhouseLoaderAvailClickhouseLoader72h
        expr: |
          (
            sum(rate(http_requests_total{service="clickhouse-loader",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="clickhouse-loader"}[72h]))
          ) > 0.005000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/clickhouse-loader-avail-clickhouse-loader
          tier: tier-2
        annotations:
          summary: "@domio/clickhouse-loader avail-clickhouse-loader burning budget at >1× over 72h"
          runbook: "runbooks/clickhouse-loader/availability.md"
- name: slo-burn-rate-@domio/analytics-service-avail-analytics
  rules:
      - alert: SLOBurnHighT2AnalyticsAvailAnalytics1h
        expr: |
          (
            sum(rate(http_requests_total{service="analytics",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="analytics"}[1h]))
          ) > 0.072000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/analytics-service-avail-analytics
          tier: tier-2
        annotations:
          summary: "@domio/analytics-service avail-analytics burning budget at >14.4× over 1h"
          runbook: "runbooks/analytics-service/availability.md"
      - alert: SLOBurnMedT2AnalyticsAvailAnalytics6h
        expr: |
          (
            sum(rate(http_requests_total{service="analytics",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="analytics"}[6h]))
          ) > 0.030000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/analytics-service-avail-analytics
          tier: tier-2
        annotations:
          summary: "@domio/analytics-service avail-analytics burning budget at >6× over 6h"
          runbook: "runbooks/analytics-service/availability.md"
      - alert: SLOBurnLowT2AnalyticsAvailAnalytics24h
        expr: |
          (
            sum(rate(http_requests_total{service="analytics",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="analytics"}[24h]))
          ) > 0.015000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/analytics-service-avail-analytics
          tier: tier-2
        annotations:
          summary: "@domio/analytics-service avail-analytics burning budget at >3× over 24h"
          runbook: "runbooks/analytics-service/availability.md"
      - alert: SLOBurnLowT2AnalyticsAvailAnalytics72h
        expr: |
          (
            sum(rate(http_requests_total{service="analytics",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="analytics"}[72h]))
          ) > 0.005000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/analytics-service-avail-analytics
          tier: tier-2
        annotations:
          summary: "@domio/analytics-service avail-analytics burning budget at >1× over 72h"
          runbook: "runbooks/analytics-service/availability.md"
- name: slo-burn-rate-@domio/live-analytics-lat-live-analytics-p95
  rules:
      - alert: SLOBurnHighT2LiveAnalyticsLatLiveAnalyticsP951h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="live-analytics",le="1"}[1h]))
              /
              sum(rate(http_request_duration_seconds_count{service="live-analytics"}[1h]))
            )
          ) > 0.720000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/live-analytics-lat-live-analytics-p95
          tier: tier-2
        annotations:
          summary: "@domio/live-analytics lat-live-analytics-p95 burning budget at >14.4× over 1h"
          runbook: "runbooks/live-analytics/latency.md"
      - alert: SLOBurnMedT2LiveAnalyticsLatLiveAnalyticsP956h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="live-analytics",le="1"}[6h]))
              /
              sum(rate(http_request_duration_seconds_count{service="live-analytics"}[6h]))
            )
          ) > 0.300000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/live-analytics-lat-live-analytics-p95
          tier: tier-2
        annotations:
          summary: "@domio/live-analytics lat-live-analytics-p95 burning budget at >6× over 6h"
          runbook: "runbooks/live-analytics/latency.md"
      - alert: SLOBurnLowT2LiveAnalyticsLatLiveAnalyticsP9524h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="live-analytics",le="1"}[24h]))
              /
              sum(rate(http_request_duration_seconds_count{service="live-analytics"}[24h]))
            )
          ) > 0.150000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/live-analytics-lat-live-analytics-p95
          tier: tier-2
        annotations:
          summary: "@domio/live-analytics lat-live-analytics-p95 burning budget at >3× over 24h"
          runbook: "runbooks/live-analytics/latency.md"
      - alert: SLOBurnLowT2LiveAnalyticsLatLiveAnalyticsP9572h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="live-analytics",le="1"}[72h]))
              /
              sum(rate(http_request_duration_seconds_count{service="live-analytics"}[72h]))
            )
          ) > 0.050000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/live-analytics-lat-live-analytics-p95
          tier: tier-2
        annotations:
          summary: "@domio/live-analytics lat-live-analytics-p95 burning budget at >1× over 72h"
          runbook: "runbooks/live-analytics/latency.md"
- name: slo-burn-rate-@domio/team-analytics-avail-team-analytics
  rules:
      - alert: SLOBurnHighT2TeamAnalyticsAvailTeamAnalytics1h
        expr: |
          (
            sum(rate(http_requests_total{service="team-analytics",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="team-analytics"}[1h]))
          ) > 0.072000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/team-analytics-avail-team-analytics
          tier: tier-2
        annotations:
          summary: "@domio/team-analytics avail-team-analytics burning budget at >14.4× over 1h"
          runbook: "runbooks/team-analytics/availability.md"
      - alert: SLOBurnMedT2TeamAnalyticsAvailTeamAnalytics6h
        expr: |
          (
            sum(rate(http_requests_total{service="team-analytics",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="team-analytics"}[6h]))
          ) > 0.030000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/team-analytics-avail-team-analytics
          tier: tier-2
        annotations:
          summary: "@domio/team-analytics avail-team-analytics burning budget at >6× over 6h"
          runbook: "runbooks/team-analytics/availability.md"
      - alert: SLOBurnLowT2TeamAnalyticsAvailTeamAnalytics24h
        expr: |
          (
            sum(rate(http_requests_total{service="team-analytics",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="team-analytics"}[24h]))
          ) > 0.015000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/team-analytics-avail-team-analytics
          tier: tier-2
        annotations:
          summary: "@domio/team-analytics avail-team-analytics burning budget at >3× over 24h"
          runbook: "runbooks/team-analytics/availability.md"
      - alert: SLOBurnLowT2TeamAnalyticsAvailTeamAnalytics72h
        expr: |
          (
            sum(rate(http_requests_total{service="team-analytics",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="team-analytics"}[72h]))
          ) > 0.005000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/team-analytics-avail-team-analytics
          tier: tier-2
        annotations:
          summary: "@domio/team-analytics avail-team-analytics burning budget at >1× over 72h"
          runbook: "runbooks/team-analytics/availability.md"
- name: slo-burn-rate-@domio/creator-analytics-service-avail-creator-analytics
  rules:
      - alert: SLOBurnHighT2CreatorAnalyticsAvailCreatorAnalytics1h
        expr: |
          (
            sum(rate(http_requests_total{service="creator-analytics",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="creator-analytics"}[1h]))
          ) > 0.072000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/creator-analytics-service-avail-creator-analytics
          tier: tier-2
        annotations:
          summary: "@domio/creator-analytics-service avail-creator-analytics burning budget at >14.4× over 1h"
          runbook: "runbooks/creator-analytics-service/availability.md"
      - alert: SLOBurnMedT2CreatorAnalyticsAvailCreatorAnalytics6h
        expr: |
          (
            sum(rate(http_requests_total{service="creator-analytics",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="creator-analytics"}[6h]))
          ) > 0.030000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/creator-analytics-service-avail-creator-analytics
          tier: tier-2
        annotations:
          summary: "@domio/creator-analytics-service avail-creator-analytics burning budget at >6× over 6h"
          runbook: "runbooks/creator-analytics-service/availability.md"
      - alert: SLOBurnLowT2CreatorAnalyticsAvailCreatorAnalytics24h
        expr: |
          (
            sum(rate(http_requests_total{service="creator-analytics",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="creator-analytics"}[24h]))
          ) > 0.015000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/creator-analytics-service-avail-creator-analytics
          tier: tier-2
        annotations:
          summary: "@domio/creator-analytics-service avail-creator-analytics burning budget at >3× over 24h"
          runbook: "runbooks/creator-analytics-service/availability.md"
      - alert: SLOBurnLowT2CreatorAnalyticsAvailCreatorAnalytics72h
        expr: |
          (
            sum(rate(http_requests_total{service="creator-analytics",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="creator-analytics"}[72h]))
          ) > 0.005000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/creator-analytics-service-avail-creator-analytics
          tier: tier-2
        annotations:
          summary: "@domio/creator-analytics-service avail-creator-analytics burning budget at >1× over 72h"
          runbook: "runbooks/creator-analytics-service/availability.md"
- name: slo-burn-rate-@domio/heatmap-generator-avail-heatmap-generator
  rules:
      - alert: SLOBurnHighT3HeatmapGeneratorAvailHeatmapGenerator1h
        expr: |
          (
            sum(rate(http_requests_total{service="heatmap-generator",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="heatmap-generator"}[1h]))
          ) > 0.144000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/heatmap-generator-avail-heatmap-generator
          tier: tier-3
        annotations:
          summary: "@domio/heatmap-generator avail-heatmap-generator burning budget at >14.4× over 1h"
          runbook: "runbooks/heatmap-generator/availability.md"
      - alert: SLOBurnMedT3HeatmapGeneratorAvailHeatmapGenerator6h
        expr: |
          (
            sum(rate(http_requests_total{service="heatmap-generator",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="heatmap-generator"}[6h]))
          ) > 0.060000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/heatmap-generator-avail-heatmap-generator
          tier: tier-3
        annotations:
          summary: "@domio/heatmap-generator avail-heatmap-generator burning budget at >6× over 6h"
          runbook: "runbooks/heatmap-generator/availability.md"
      - alert: SLOBurnLowT3HeatmapGeneratorAvailHeatmapGenerator24h
        expr: |
          (
            sum(rate(http_requests_total{service="heatmap-generator",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="heatmap-generator"}[24h]))
          ) > 0.030000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/heatmap-generator-avail-heatmap-generator
          tier: tier-3
        annotations:
          summary: "@domio/heatmap-generator avail-heatmap-generator burning budget at >3× over 24h"
          runbook: "runbooks/heatmap-generator/availability.md"
      - alert: SLOBurnLowT3HeatmapGeneratorAvailHeatmapGenerator72h
        expr: |
          (
            sum(rate(http_requests_total{service="heatmap-generator",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="heatmap-generator"}[72h]))
          ) > 0.010000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/heatmap-generator-avail-heatmap-generator
          tier: tier-3
        annotations:
          summary: "@domio/heatmap-generator avail-heatmap-generator burning budget at >1× over 72h"
          runbook: "runbooks/heatmap-generator/availability.md"
- name: slo-burn-rate-@domio/sessionization-avail-sessionization
  rules:
      - alert: SLOBurnHighT3SessionizationAvailSessionization1h
        expr: |
          (
            sum(rate(http_requests_total{service="sessionization",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="sessionization"}[1h]))
          ) > 0.144000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/sessionization-avail-sessionization
          tier: tier-3
        annotations:
          summary: "@domio/sessionization avail-sessionization burning budget at >14.4× over 1h"
          runbook: "runbooks/sessionization/availability.md"
      - alert: SLOBurnMedT3SessionizationAvailSessionization6h
        expr: |
          (
            sum(rate(http_requests_total{service="sessionization",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="sessionization"}[6h]))
          ) > 0.060000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/sessionization-avail-sessionization
          tier: tier-3
        annotations:
          summary: "@domio/sessionization avail-sessionization burning budget at >6× over 6h"
          runbook: "runbooks/sessionization/availability.md"
      - alert: SLOBurnLowT3SessionizationAvailSessionization24h
        expr: |
          (
            sum(rate(http_requests_total{service="sessionization",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="sessionization"}[24h]))
          ) > 0.030000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/sessionization-avail-sessionization
          tier: tier-3
        annotations:
          summary: "@domio/sessionization avail-sessionization burning budget at >3× over 24h"
          runbook: "runbooks/sessionization/availability.md"
      - alert: SLOBurnLowT3SessionizationAvailSessionization72h
        expr: |
          (
            sum(rate(http_requests_total{service="sessionization",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="sessionization"}[72h]))
          ) > 0.010000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/sessionization-avail-sessionization
          tier: tier-3
        annotations:
          summary: "@domio/sessionization avail-sessionization burning budget at >1× over 72h"
          runbook: "runbooks/sessionization/availability.md"
- name: slo-burn-rate-@domio/notification-dispatcher-avail-notification-dispatcher
  rules:
      - alert: SLOBurnHighT2NotificationDispatcherAvailNotificationDispatcher1h
        expr: |
          (
            sum(rate(http_requests_total{service="notification-dispatcher",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="notification-dispatcher"}[1h]))
          ) > 0.072000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/notification-dispatcher-avail-notification-dispatcher
          tier: tier-2
        annotations:
          summary: "@domio/notification-dispatcher avail-notification-dispatcher burning budget at >14.4× over 1h"
          runbook: "runbooks/notification-dispatcher/availability.md"
      - alert: SLOBurnMedT2NotificationDispatcherAvailNotificationDispatcher6h
        expr: |
          (
            sum(rate(http_requests_total{service="notification-dispatcher",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="notification-dispatcher"}[6h]))
          ) > 0.030000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/notification-dispatcher-avail-notification-dispatcher
          tier: tier-2
        annotations:
          summary: "@domio/notification-dispatcher avail-notification-dispatcher burning budget at >6× over 6h"
          runbook: "runbooks/notification-dispatcher/availability.md"
      - alert: SLOBurnLowT2NotificationDispatcherAvailNotificationDispatcher24h
        expr: |
          (
            sum(rate(http_requests_total{service="notification-dispatcher",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="notification-dispatcher"}[24h]))
          ) > 0.015000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/notification-dispatcher-avail-notification-dispatcher
          tier: tier-2
        annotations:
          summary: "@domio/notification-dispatcher avail-notification-dispatcher burning budget at >3× over 24h"
          runbook: "runbooks/notification-dispatcher/availability.md"
      - alert: SLOBurnLowT2NotificationDispatcherAvailNotificationDispatcher72h
        expr: |
          (
            sum(rate(http_requests_total{service="notification-dispatcher",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="notification-dispatcher"}[72h]))
          ) > 0.005000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/notification-dispatcher-avail-notification-dispatcher
          tier: tier-2
        annotations:
          summary: "@domio/notification-dispatcher avail-notification-dispatcher burning budget at >1× over 72h"
          runbook: "runbooks/notification-dispatcher/availability.md"
- name: slo-burn-rate-@domio/edge-pubsub-avail-edge-pubsub
  rules:
      - alert: SLOBurnHighT1EdgePubsubAvailEdgePubsub1h
        expr: |
          (
            sum(rate(http_requests_total{service="edge-pubsub",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="edge-pubsub"}[1h]))
          ) > 0.014400
        for: 2m
        labels:
          severity: page
          slo: @domio/edge-pubsub-avail-edge-pubsub
          tier: tier-1
        annotations:
          summary: "@domio/edge-pubsub avail-edge-pubsub burning budget at >14.4× over 1h"
          runbook: "runbooks/edge-pubsub/availability.md"
      - alert: SLOBurnMedT1EdgePubsubAvailEdgePubsub6h
        expr: |
          (
            sum(rate(http_requests_total{service="edge-pubsub",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="edge-pubsub"}[6h]))
          ) > 0.006000
        for: 5m
        labels:
          severity: page
          slo: @domio/edge-pubsub-avail-edge-pubsub
          tier: tier-1
        annotations:
          summary: "@domio/edge-pubsub avail-edge-pubsub burning budget at >6× over 6h"
          runbook: "runbooks/edge-pubsub/availability.md"
      - alert: SLOBurnLowT1EdgePubsubAvailEdgePubsub24h
        expr: |
          (
            sum(rate(http_requests_total{service="edge-pubsub",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="edge-pubsub"}[24h]))
          ) > 0.003000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/edge-pubsub-avail-edge-pubsub
          tier: tier-1
        annotations:
          summary: "@domio/edge-pubsub avail-edge-pubsub burning budget at >3× over 24h"
          runbook: "runbooks/edge-pubsub/availability.md"
      - alert: SLOBurnLowT1EdgePubsubAvailEdgePubsub72h
        expr: |
          (
            sum(rate(http_requests_total{service="edge-pubsub",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="edge-pubsub"}[72h]))
          ) > 0.001000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/edge-pubsub-avail-edge-pubsub
          tier: tier-1
        annotations:
          summary: "@domio/edge-pubsub avail-edge-pubsub burning budget at >1× over 72h"
          runbook: "runbooks/edge-pubsub/availability.md"
- name: slo-burn-rate-@domio/session-coordinator-avail-session-coordinator
  rules:
      - alert: SLOBurnHighT1SessionCoordinatorAvailSessionCoordinator1h
        expr: |
          (
            sum(rate(http_requests_total{service="session-coordinator",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="session-coordinator"}[1h]))
          ) > 0.014400
        for: 2m
        labels:
          severity: page
          slo: @domio/session-coordinator-avail-session-coordinator
          tier: tier-1
        annotations:
          summary: "@domio/session-coordinator avail-session-coordinator burning budget at >14.4× over 1h"
          runbook: "runbooks/session-coordinator/availability.md"
      - alert: SLOBurnMedT1SessionCoordinatorAvailSessionCoordinator6h
        expr: |
          (
            sum(rate(http_requests_total{service="session-coordinator",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="session-coordinator"}[6h]))
          ) > 0.006000
        for: 5m
        labels:
          severity: page
          slo: @domio/session-coordinator-avail-session-coordinator
          tier: tier-1
        annotations:
          summary: "@domio/session-coordinator avail-session-coordinator burning budget at >6× over 6h"
          runbook: "runbooks/session-coordinator/availability.md"
      - alert: SLOBurnLowT1SessionCoordinatorAvailSessionCoordinator24h
        expr: |
          (
            sum(rate(http_requests_total{service="session-coordinator",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="session-coordinator"}[24h]))
          ) > 0.003000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/session-coordinator-avail-session-coordinator
          tier: tier-1
        annotations:
          summary: "@domio/session-coordinator avail-session-coordinator burning budget at >3× over 24h"
          runbook: "runbooks/session-coordinator/availability.md"
      - alert: SLOBurnLowT1SessionCoordinatorAvailSessionCoordinator72h
        expr: |
          (
            sum(rate(http_requests_total{service="session-coordinator",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="session-coordinator"}[72h]))
          ) > 0.001000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/session-coordinator-avail-session-coordinator
          tier: tier-1
        annotations:
          summary: "@domio/session-coordinator avail-session-coordinator burning budget at >1× over 72h"
          runbook: "runbooks/session-coordinator/availability.md"
- name: slo-burn-rate-@domio/participant-session-avail-participant-session
  rules:
      - alert: SLOBurnHighT1ParticipantSessionAvailParticipantSession1h
        expr: |
          (
            sum(rate(http_requests_total{service="participant-session",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="participant-session"}[1h]))
          ) > 0.014400
        for: 2m
        labels:
          severity: page
          slo: @domio/participant-session-avail-participant-session
          tier: tier-1
        annotations:
          summary: "@domio/participant-session avail-participant-session burning budget at >14.4× over 1h"
          runbook: "runbooks/participant-session/availability.md"
      - alert: SLOBurnMedT1ParticipantSessionAvailParticipantSession6h
        expr: |
          (
            sum(rate(http_requests_total{service="participant-session",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="participant-session"}[6h]))
          ) > 0.006000
        for: 5m
        labels:
          severity: page
          slo: @domio/participant-session-avail-participant-session
          tier: tier-1
        annotations:
          summary: "@domio/participant-session avail-participant-session burning budget at >6× over 6h"
          runbook: "runbooks/participant-session/availability.md"
      - alert: SLOBurnLowT1ParticipantSessionAvailParticipantSession24h
        expr: |
          (
            sum(rate(http_requests_total{service="participant-session",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="participant-session"}[24h]))
          ) > 0.003000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/participant-session-avail-participant-session
          tier: tier-1
        annotations:
          summary: "@domio/participant-session avail-participant-session burning budget at >3× over 24h"
          runbook: "runbooks/participant-session/availability.md"
      - alert: SLOBurnLowT1ParticipantSessionAvailParticipantSession72h
        expr: |
          (
            sum(rate(http_requests_total{service="participant-session",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="participant-session"}[72h]))
          ) > 0.001000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/participant-session-avail-participant-session
          tier: tier-1
        annotations:
          summary: "@domio/participant-session avail-participant-session burning budget at >1× over 72h"
          runbook: "runbooks/participant-session/availability.md"
- name: slo-burn-rate-@domio/collab-service-avail-collab
  rules:
      - alert: SLOBurnHighT1CollabAvailCollab1h
        expr: |
          (
            sum(rate(http_requests_total{service="collab",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="collab"}[1h]))
          ) > 0.014400
        for: 2m
        labels:
          severity: page
          slo: @domio/collab-service-avail-collab
          tier: tier-1
        annotations:
          summary: "@domio/collab-service avail-collab burning budget at >14.4× over 1h"
          runbook: "runbooks/collab-service/availability.md"
      - alert: SLOBurnMedT1CollabAvailCollab6h
        expr: |
          (
            sum(rate(http_requests_total{service="collab",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="collab"}[6h]))
          ) > 0.006000
        for: 5m
        labels:
          severity: page
          slo: @domio/collab-service-avail-collab
          tier: tier-1
        annotations:
          summary: "@domio/collab-service avail-collab burning budget at >6× over 6h"
          runbook: "runbooks/collab-service/availability.md"
      - alert: SLOBurnLowT1CollabAvailCollab24h
        expr: |
          (
            sum(rate(http_requests_total{service="collab",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="collab"}[24h]))
          ) > 0.003000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/collab-service-avail-collab
          tier: tier-1
        annotations:
          summary: "@domio/collab-service avail-collab burning budget at >3× over 24h"
          runbook: "runbooks/collab-service/availability.md"
      - alert: SLOBurnLowT1CollabAvailCollab72h
        expr: |
          (
            sum(rate(http_requests_total{service="collab",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="collab"}[72h]))
          ) > 0.001000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/collab-service-avail-collab
          tier: tier-1
        annotations:
          summary: "@domio/collab-service avail-collab burning budget at >1× over 72h"
          runbook: "runbooks/collab-service/availability.md"
- name: slo-burn-rate-@domio/merge-request-service-avail-merge-requests
  rules:
      - alert: SLOBurnHighT2MergeRequestAvailMergeRequests1h
        expr: |
          (
            sum(rate(http_requests_total{service="merge-request",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="merge-request"}[1h]))
          ) > 0.072000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/merge-request-service-avail-merge-requests
          tier: tier-2
        annotations:
          summary: "@domio/merge-request-service avail-merge-requests burning budget at >14.4× over 1h"
          runbook: "runbooks/merge-request-service/availability.md"
      - alert: SLOBurnMedT2MergeRequestAvailMergeRequests6h
        expr: |
          (
            sum(rate(http_requests_total{service="merge-request",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="merge-request"}[6h]))
          ) > 0.030000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/merge-request-service-avail-merge-requests
          tier: tier-2
        annotations:
          summary: "@domio/merge-request-service avail-merge-requests burning budget at >6× over 6h"
          runbook: "runbooks/merge-request-service/availability.md"
      - alert: SLOBurnLowT2MergeRequestAvailMergeRequests24h
        expr: |
          (
            sum(rate(http_requests_total{service="merge-request",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="merge-request"}[24h]))
          ) > 0.015000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/merge-request-service-avail-merge-requests
          tier: tier-2
        annotations:
          summary: "@domio/merge-request-service avail-merge-requests burning budget at >3× over 24h"
          runbook: "runbooks/merge-request-service/availability.md"
      - alert: SLOBurnLowT2MergeRequestAvailMergeRequests72h
        expr: |
          (
            sum(rate(http_requests_total{service="merge-request",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="merge-request"}[72h]))
          ) > 0.005000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/merge-request-service-avail-merge-requests
          tier: tier-2
        annotations:
          summary: "@domio/merge-request-service avail-merge-requests burning budget at >1× over 72h"
          runbook: "runbooks/merge-request-service/availability.md"
- name: slo-burn-rate-@domio/task-manager-service-avail-task-manager
  rules:
      - alert: SLOBurnHighT2TaskManagerAvailTaskManager1h
        expr: |
          (
            sum(rate(http_requests_total{service="task-manager",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="task-manager"}[1h]))
          ) > 0.072000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/task-manager-service-avail-task-manager
          tier: tier-2
        annotations:
          summary: "@domio/task-manager-service avail-task-manager burning budget at >14.4× over 1h"
          runbook: "runbooks/task-manager-service/availability.md"
      - alert: SLOBurnMedT2TaskManagerAvailTaskManager6h
        expr: |
          (
            sum(rate(http_requests_total{service="task-manager",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="task-manager"}[6h]))
          ) > 0.030000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/task-manager-service-avail-task-manager
          tier: tier-2
        annotations:
          summary: "@domio/task-manager-service avail-task-manager burning budget at >6× over 6h"
          runbook: "runbooks/task-manager-service/availability.md"
      - alert: SLOBurnLowT2TaskManagerAvailTaskManager24h
        expr: |
          (
            sum(rate(http_requests_total{service="task-manager",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="task-manager"}[24h]))
          ) > 0.015000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/task-manager-service-avail-task-manager
          tier: tier-2
        annotations:
          summary: "@domio/task-manager-service avail-task-manager burning budget at >3× over 24h"
          runbook: "runbooks/task-manager-service/availability.md"
      - alert: SLOBurnLowT2TaskManagerAvailTaskManager72h
        expr: |
          (
            sum(rate(http_requests_total{service="task-manager",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="task-manager"}[72h]))
          ) > 0.005000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/task-manager-service-avail-task-manager
          tier: tier-2
        annotations:
          summary: "@domio/task-manager-service avail-task-manager burning budget at >1× over 72h"
          runbook: "runbooks/task-manager-service/availability.md"
- name: slo-burn-rate-@domio/library-service-avail-library
  rules:
      - alert: SLOBurnHighT1LibraryAvailLibrary1h
        expr: |
          (
            sum(rate(http_requests_total{service="library",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="library"}[1h]))
          ) > 0.014400
        for: 2m
        labels:
          severity: page
          slo: @domio/library-service-avail-library
          tier: tier-1
        annotations:
          summary: "@domio/library-service avail-library burning budget at >14.4× over 1h"
          runbook: "runbooks/library-service/availability.md"
      - alert: SLOBurnMedT1LibraryAvailLibrary6h
        expr: |
          (
            sum(rate(http_requests_total{service="library",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="library"}[6h]))
          ) > 0.006000
        for: 5m
        labels:
          severity: page
          slo: @domio/library-service-avail-library
          tier: tier-1
        annotations:
          summary: "@domio/library-service avail-library burning budget at >6× over 6h"
          runbook: "runbooks/library-service/availability.md"
      - alert: SLOBurnLowT1LibraryAvailLibrary24h
        expr: |
          (
            sum(rate(http_requests_total{service="library",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="library"}[24h]))
          ) > 0.003000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/library-service-avail-library
          tier: tier-1
        annotations:
          summary: "@domio/library-service avail-library burning budget at >3× over 24h"
          runbook: "runbooks/library-service/availability.md"
      - alert: SLOBurnLowT1LibraryAvailLibrary72h
        expr: |
          (
            sum(rate(http_requests_total{service="library",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="library"}[72h]))
          ) > 0.001000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/library-service-avail-library
          tier: tier-1
        annotations:
          summary: "@domio/library-service avail-library burning budget at >1× over 72h"
          runbook: "runbooks/library-service/availability.md"
- name: slo-burn-rate-@domio/suggestions-service-avail-suggestions
  rules:
      - alert: SLOBurnHighT3SuggestionsAvailSuggestions1h
        expr: |
          (
            sum(rate(http_requests_total{service="suggestions",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="suggestions"}[1h]))
          ) > 0.144000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/suggestions-service-avail-suggestions
          tier: tier-3
        annotations:
          summary: "@domio/suggestions-service avail-suggestions burning budget at >14.4× over 1h"
          runbook: "runbooks/suggestions-service/availability.md"
      - alert: SLOBurnMedT3SuggestionsAvailSuggestions6h
        expr: |
          (
            sum(rate(http_requests_total{service="suggestions",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="suggestions"}[6h]))
          ) > 0.060000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/suggestions-service-avail-suggestions
          tier: tier-3
        annotations:
          summary: "@domio/suggestions-service avail-suggestions burning budget at >6× over 6h"
          runbook: "runbooks/suggestions-service/availability.md"
      - alert: SLOBurnLowT3SuggestionsAvailSuggestions24h
        expr: |
          (
            sum(rate(http_requests_total{service="suggestions",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="suggestions"}[24h]))
          ) > 0.030000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/suggestions-service-avail-suggestions
          tier: tier-3
        annotations:
          summary: "@domio/suggestions-service avail-suggestions burning budget at >3× over 24h"
          runbook: "runbooks/suggestions-service/availability.md"
      - alert: SLOBurnLowT3SuggestionsAvailSuggestions72h
        expr: |
          (
            sum(rate(http_requests_total{service="suggestions",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="suggestions"}[72h]))
          ) > 0.010000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/suggestions-service-avail-suggestions
          tier: tier-3
        annotations:
          summary: "@domio/suggestions-service avail-suggestions burning budget at >1× over 72h"
          runbook: "runbooks/suggestions-service/availability.md"
- name: slo-burn-rate-@domio/expiry-service-avail-expiry
  rules:
      - alert: SLOBurnHighT3ExpiryAvailExpiry1h
        expr: |
          (
            sum(rate(http_requests_total{service="expiry",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="expiry"}[1h]))
          ) > 0.144000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/expiry-service-avail-expiry
          tier: tier-3
        annotations:
          summary: "@domio/expiry-service avail-expiry burning budget at >14.4× over 1h"
          runbook: "runbooks/expiry-service/availability.md"
      - alert: SLOBurnMedT3ExpiryAvailExpiry6h
        expr: |
          (
            sum(rate(http_requests_total{service="expiry",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="expiry"}[6h]))
          ) > 0.060000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/expiry-service-avail-expiry
          tier: tier-3
        annotations:
          summary: "@domio/expiry-service avail-expiry burning budget at >6× over 6h"
          runbook: "runbooks/expiry-service/availability.md"
      - alert: SLOBurnLowT3ExpiryAvailExpiry24h
        expr: |
          (
            sum(rate(http_requests_total{service="expiry",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="expiry"}[24h]))
          ) > 0.030000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/expiry-service-avail-expiry
          tier: tier-3
        annotations:
          summary: "@domio/expiry-service avail-expiry burning budget at >3× over 24h"
          runbook: "runbooks/expiry-service/availability.md"
      - alert: SLOBurnLowT3ExpiryAvailExpiry72h
        expr: |
          (
            sum(rate(http_requests_total{service="expiry",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="expiry"}[72h]))
          ) > 0.010000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/expiry-service-avail-expiry
          tier: tier-3
        annotations:
          summary: "@domio/expiry-service avail-expiry burning budget at >1× over 72h"
          runbook: "runbooks/expiry-service/availability.md"
- name: slo-burn-rate-@domio/annotation-engine-avail-annotation-engine
  rules:
      - alert: SLOBurnHighT2AnnotationEngineAvailAnnotationEngine1h
        expr: |
          (
            sum(rate(http_requests_total{service="annotation-engine",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="annotation-engine"}[1h]))
          ) > 0.072000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/annotation-engine-avail-annotation-engine
          tier: tier-2
        annotations:
          summary: "@domio/annotation-engine avail-annotation-engine burning budget at >14.4× over 1h"
          runbook: "runbooks/annotation-engine/availability.md"
      - alert: SLOBurnMedT2AnnotationEngineAvailAnnotationEngine6h
        expr: |
          (
            sum(rate(http_requests_total{service="annotation-engine",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="annotation-engine"}[6h]))
          ) > 0.030000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/annotation-engine-avail-annotation-engine
          tier: tier-2
        annotations:
          summary: "@domio/annotation-engine avail-annotation-engine burning budget at >6× over 6h"
          runbook: "runbooks/annotation-engine/availability.md"
      - alert: SLOBurnLowT2AnnotationEngineAvailAnnotationEngine24h
        expr: |
          (
            sum(rate(http_requests_total{service="annotation-engine",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="annotation-engine"}[24h]))
          ) > 0.015000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/annotation-engine-avail-annotation-engine
          tier: tier-2
        annotations:
          summary: "@domio/annotation-engine avail-annotation-engine burning budget at >3× over 24h"
          runbook: "runbooks/annotation-engine/availability.md"
      - alert: SLOBurnLowT2AnnotationEngineAvailAnnotationEngine72h
        expr: |
          (
            sum(rate(http_requests_total{service="annotation-engine",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="annotation-engine"}[72h]))
          ) > 0.005000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/annotation-engine-avail-annotation-engine
          tier: tier-2
        annotations:
          summary: "@domio/annotation-engine avail-annotation-engine burning budget at >1× over 72h"
          runbook: "runbooks/annotation-engine/availability.md"
- name: slo-burn-rate-@domio/recording-orchestrator-avail-recording-orchestrator
  rules:
      - alert: SLOBurnHighT2RecordingOrchestratorAvailRecordingOrchestrator1h
        expr: |
          (
            sum(rate(http_requests_total{service="recording-orchestrator",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="recording-orchestrator"}[1h]))
          ) > 0.072000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/recording-orchestrator-avail-recording-orchestrator
          tier: tier-2
        annotations:
          summary: "@domio/recording-orchestrator avail-recording-orchestrator burning budget at >14.4× over 1h"
          runbook: "runbooks/recording-orchestrator/availability.md"
      - alert: SLOBurnMedT2RecordingOrchestratorAvailRecordingOrchestrator6h
        expr: |
          (
            sum(rate(http_requests_total{service="recording-orchestrator",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="recording-orchestrator"}[6h]))
          ) > 0.030000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/recording-orchestrator-avail-recording-orchestrator
          tier: tier-2
        annotations:
          summary: "@domio/recording-orchestrator avail-recording-orchestrator burning budget at >6× over 6h"
          runbook: "runbooks/recording-orchestrator/availability.md"
      - alert: SLOBurnLowT2RecordingOrchestratorAvailRecordingOrchestrator24h
        expr: |
          (
            sum(rate(http_requests_total{service="recording-orchestrator",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="recording-orchestrator"}[24h]))
          ) > 0.015000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/recording-orchestrator-avail-recording-orchestrator
          tier: tier-2
        annotations:
          summary: "@domio/recording-orchestrator avail-recording-orchestrator burning budget at >3× over 24h"
          runbook: "runbooks/recording-orchestrator/availability.md"
      - alert: SLOBurnLowT2RecordingOrchestratorAvailRecordingOrchestrator72h
        expr: |
          (
            sum(rate(http_requests_total{service="recording-orchestrator",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="recording-orchestrator"}[72h]))
          ) > 0.005000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/recording-orchestrator-avail-recording-orchestrator
          tier: tier-2
        annotations:
          summary: "@domio/recording-orchestrator avail-recording-orchestrator burning budget at >1× over 72h"
          runbook: "runbooks/recording-orchestrator/availability.md"
- name: slo-burn-rate-@domio/reaction-broadcaster-avail-reaction-broadcaster
  rules:
      - alert: SLOBurnHighT2ReactionBroadcasterAvailReactionBroadcaster1h
        expr: |
          (
            sum(rate(http_requests_total{service="reaction-broadcaster",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="reaction-broadcaster"}[1h]))
          ) > 0.072000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/reaction-broadcaster-avail-reaction-broadcaster
          tier: tier-2
        annotations:
          summary: "@domio/reaction-broadcaster avail-reaction-broadcaster burning budget at >14.4× over 1h"
          runbook: "runbooks/reaction-broadcaster/availability.md"
      - alert: SLOBurnMedT2ReactionBroadcasterAvailReactionBroadcaster6h
        expr: |
          (
            sum(rate(http_requests_total{service="reaction-broadcaster",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="reaction-broadcaster"}[6h]))
          ) > 0.030000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/reaction-broadcaster-avail-reaction-broadcaster
          tier: tier-2
        annotations:
          summary: "@domio/reaction-broadcaster avail-reaction-broadcaster burning budget at >6× over 6h"
          runbook: "runbooks/reaction-broadcaster/availability.md"
      - alert: SLOBurnLowT2ReactionBroadcasterAvailReactionBroadcaster24h
        expr: |
          (
            sum(rate(http_requests_total{service="reaction-broadcaster",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="reaction-broadcaster"}[24h]))
          ) > 0.015000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/reaction-broadcaster-avail-reaction-broadcaster
          tier: tier-2
        annotations:
          summary: "@domio/reaction-broadcaster avail-reaction-broadcaster burning budget at >3× over 24h"
          runbook: "runbooks/reaction-broadcaster/availability.md"
      - alert: SLOBurnLowT2ReactionBroadcasterAvailReactionBroadcaster72h
        expr: |
          (
            sum(rate(http_requests_total{service="reaction-broadcaster",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="reaction-broadcaster"}[72h]))
          ) > 0.005000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/reaction-broadcaster-avail-reaction-broadcaster
          tier: tier-2
        annotations:
          summary: "@domio/reaction-broadcaster avail-reaction-broadcaster burning budget at >1× over 72h"
          runbook: "runbooks/reaction-broadcaster/availability.md"
- name: slo-burn-rate-@domio/poll-engine-avail-poll-engine
  rules:
      - alert: SLOBurnHighT2PollEngineAvailPollEngine1h
        expr: |
          (
            sum(rate(http_requests_total{service="poll-engine",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="poll-engine"}[1h]))
          ) > 0.072000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/poll-engine-avail-poll-engine
          tier: tier-2
        annotations:
          summary: "@domio/poll-engine avail-poll-engine burning budget at >14.4× over 1h"
          runbook: "runbooks/poll-engine/availability.md"
      - alert: SLOBurnMedT2PollEngineAvailPollEngine6h
        expr: |
          (
            sum(rate(http_requests_total{service="poll-engine",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="poll-engine"}[6h]))
          ) > 0.030000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/poll-engine-avail-poll-engine
          tier: tier-2
        annotations:
          summary: "@domio/poll-engine avail-poll-engine burning budget at >6× over 6h"
          runbook: "runbooks/poll-engine/availability.md"
      - alert: SLOBurnLowT2PollEngineAvailPollEngine24h
        expr: |
          (
            sum(rate(http_requests_total{service="poll-engine",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="poll-engine"}[24h]))
          ) > 0.015000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/poll-engine-avail-poll-engine
          tier: tier-2
        annotations:
          summary: "@domio/poll-engine avail-poll-engine burning budget at >3× over 24h"
          runbook: "runbooks/poll-engine/availability.md"
      - alert: SLOBurnLowT2PollEngineAvailPollEngine72h
        expr: |
          (
            sum(rate(http_requests_total{service="poll-engine",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="poll-engine"}[72h]))
          ) > 0.005000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/poll-engine-avail-poll-engine
          tier: tier-2
        annotations:
          summary: "@domio/poll-engine avail-poll-engine burning budget at >1× over 72h"
          runbook: "runbooks/poll-engine/availability.md"
- name: slo-burn-rate-@domio/quiz-engine-avail-quiz-engine
  rules:
      - alert: SLOBurnHighT2QuizEngineAvailQuizEngine1h
        expr: |
          (
            sum(rate(http_requests_total{service="quiz-engine",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="quiz-engine"}[1h]))
          ) > 0.072000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/quiz-engine-avail-quiz-engine
          tier: tier-2
        annotations:
          summary: "@domio/quiz-engine avail-quiz-engine burning budget at >14.4× over 1h"
          runbook: "runbooks/quiz-engine/availability.md"
      - alert: SLOBurnMedT2QuizEngineAvailQuizEngine6h
        expr: |
          (
            sum(rate(http_requests_total{service="quiz-engine",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="quiz-engine"}[6h]))
          ) > 0.030000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/quiz-engine-avail-quiz-engine
          tier: tier-2
        annotations:
          summary: "@domio/quiz-engine avail-quiz-engine burning budget at >6× over 6h"
          runbook: "runbooks/quiz-engine/availability.md"
      - alert: SLOBurnLowT2QuizEngineAvailQuizEngine24h
        expr: |
          (
            sum(rate(http_requests_total{service="quiz-engine",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="quiz-engine"}[24h]))
          ) > 0.015000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/quiz-engine-avail-quiz-engine
          tier: tier-2
        annotations:
          summary: "@domio/quiz-engine avail-quiz-engine burning budget at >3× over 24h"
          runbook: "runbooks/quiz-engine/availability.md"
      - alert: SLOBurnLowT2QuizEngineAvailQuizEngine72h
        expr: |
          (
            sum(rate(http_requests_total{service="quiz-engine",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="quiz-engine"}[72h]))
          ) > 0.005000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/quiz-engine-avail-quiz-engine
          tier: tier-2
        annotations:
          summary: "@domio/quiz-engine avail-quiz-engine burning budget at >1× over 72h"
          runbook: "runbooks/quiz-engine/availability.md"
- name: slo-burn-rate-@domio/qa-engine-avail-qa-engine
  rules:
      - alert: SLOBurnHighT2QaEngineAvailQaEngine1h
        expr: |
          (
            sum(rate(http_requests_total{service="qa-engine",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="qa-engine"}[1h]))
          ) > 0.072000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/qa-engine-avail-qa-engine
          tier: tier-2
        annotations:
          summary: "@domio/qa-engine avail-qa-engine burning budget at >14.4× over 1h"
          runbook: "runbooks/qa-engine/availability.md"
      - alert: SLOBurnMedT2QaEngineAvailQaEngine6h
        expr: |
          (
            sum(rate(http_requests_total{service="qa-engine",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="qa-engine"}[6h]))
          ) > 0.030000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/qa-engine-avail-qa-engine
          tier: tier-2
        annotations:
          summary: "@domio/qa-engine avail-qa-engine burning budget at >6× over 6h"
          runbook: "runbooks/qa-engine/availability.md"
      - alert: SLOBurnLowT2QaEngineAvailQaEngine24h
        expr: |
          (
            sum(rate(http_requests_total{service="qa-engine",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="qa-engine"}[24h]))
          ) > 0.015000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/qa-engine-avail-qa-engine
          tier: tier-2
        annotations:
          summary: "@domio/qa-engine avail-qa-engine burning budget at >3× over 24h"
          runbook: "runbooks/qa-engine/availability.md"
      - alert: SLOBurnLowT2QaEngineAvailQaEngine72h
        expr: |
          (
            sum(rate(http_requests_total{service="qa-engine",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="qa-engine"}[72h]))
          ) > 0.005000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/qa-engine-avail-qa-engine
          tier: tier-2
        annotations:
          summary: "@domio/qa-engine avail-qa-engine burning budget at >1× over 72h"
          runbook: "runbooks/qa-engine/availability.md"
- name: slo-burn-rate-@domio/word-cloud-engine-avail-word-cloud-engine
  rules:
      - alert: SLOBurnHighT2WordCloudEngineAvailWordCloudEngine1h
        expr: |
          (
            sum(rate(http_requests_total{service="word-cloud-engine",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="word-cloud-engine"}[1h]))
          ) > 0.072000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/word-cloud-engine-avail-word-cloud-engine
          tier: tier-2
        annotations:
          summary: "@domio/word-cloud-engine avail-word-cloud-engine burning budget at >14.4× over 1h"
          runbook: "runbooks/word-cloud-engine/availability.md"
      - alert: SLOBurnMedT2WordCloudEngineAvailWordCloudEngine6h
        expr: |
          (
            sum(rate(http_requests_total{service="word-cloud-engine",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="word-cloud-engine"}[6h]))
          ) > 0.030000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/word-cloud-engine-avail-word-cloud-engine
          tier: tier-2
        annotations:
          summary: "@domio/word-cloud-engine avail-word-cloud-engine burning budget at >6× over 6h"
          runbook: "runbooks/word-cloud-engine/availability.md"
      - alert: SLOBurnLowT2WordCloudEngineAvailWordCloudEngine24h
        expr: |
          (
            sum(rate(http_requests_total{service="word-cloud-engine",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="word-cloud-engine"}[24h]))
          ) > 0.015000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/word-cloud-engine-avail-word-cloud-engine
          tier: tier-2
        annotations:
          summary: "@domio/word-cloud-engine avail-word-cloud-engine burning budget at >3× over 24h"
          runbook: "runbooks/word-cloud-engine/availability.md"
      - alert: SLOBurnLowT2WordCloudEngineAvailWordCloudEngine72h
        expr: |
          (
            sum(rate(http_requests_total{service="word-cloud-engine",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="word-cloud-engine"}[72h]))
          ) > 0.005000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/word-cloud-engine-avail-word-cloud-engine
          tier: tier-2
        annotations:
          summary: "@domio/word-cloud-engine avail-word-cloud-engine burning budget at >1× over 72h"
          runbook: "runbooks/word-cloud-engine/availability.md"
- name: slo-burn-rate-@domio/raise-hand-queue-avail-raise-hand-queue
  rules:
      - alert: SLOBurnHighT2RaiseHandQueueAvailRaiseHandQueue1h
        expr: |
          (
            sum(rate(http_requests_total{service="raise-hand-queue",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="raise-hand-queue"}[1h]))
          ) > 0.072000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/raise-hand-queue-avail-raise-hand-queue
          tier: tier-2
        annotations:
          summary: "@domio/raise-hand-queue avail-raise-hand-queue burning budget at >14.4× over 1h"
          runbook: "runbooks/raise-hand-queue/availability.md"
      - alert: SLOBurnMedT2RaiseHandQueueAvailRaiseHandQueue6h
        expr: |
          (
            sum(rate(http_requests_total{service="raise-hand-queue",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="raise-hand-queue"}[6h]))
          ) > 0.030000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/raise-hand-queue-avail-raise-hand-queue
          tier: tier-2
        annotations:
          summary: "@domio/raise-hand-queue avail-raise-hand-queue burning budget at >6× over 6h"
          runbook: "runbooks/raise-hand-queue/availability.md"
      - alert: SLOBurnLowT2RaiseHandQueueAvailRaiseHandQueue24h
        expr: |
          (
            sum(rate(http_requests_total{service="raise-hand-queue",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="raise-hand-queue"}[24h]))
          ) > 0.015000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/raise-hand-queue-avail-raise-hand-queue
          tier: tier-2
        annotations:
          summary: "@domio/raise-hand-queue avail-raise-hand-queue burning budget at >3× over 24h"
          runbook: "runbooks/raise-hand-queue/availability.md"
      - alert: SLOBurnLowT2RaiseHandQueueAvailRaiseHandQueue72h
        expr: |
          (
            sum(rate(http_requests_total{service="raise-hand-queue",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="raise-hand-queue"}[72h]))
          ) > 0.005000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/raise-hand-queue-avail-raise-hand-queue
          tier: tier-2
        annotations:
          summary: "@domio/raise-hand-queue avail-raise-hand-queue burning budget at >1× over 72h"
          runbook: "runbooks/raise-hand-queue/availability.md"
- name: slo-burn-rate-@domio/nav-vote-collector-avail-nav-vote-collector
  rules:
      - alert: SLOBurnHighT2NavVoteCollectorAvailNavVoteCollector1h
        expr: |
          (
            sum(rate(http_requests_total{service="nav-vote-collector",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="nav-vote-collector"}[1h]))
          ) > 0.072000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/nav-vote-collector-avail-nav-vote-collector
          tier: tier-2
        annotations:
          summary: "@domio/nav-vote-collector avail-nav-vote-collector burning budget at >14.4× over 1h"
          runbook: "runbooks/nav-vote-collector/availability.md"
      - alert: SLOBurnMedT2NavVoteCollectorAvailNavVoteCollector6h
        expr: |
          (
            sum(rate(http_requests_total{service="nav-vote-collector",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="nav-vote-collector"}[6h]))
          ) > 0.030000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/nav-vote-collector-avail-nav-vote-collector
          tier: tier-2
        annotations:
          summary: "@domio/nav-vote-collector avail-nav-vote-collector burning budget at >6× over 6h"
          runbook: "runbooks/nav-vote-collector/availability.md"
      - alert: SLOBurnLowT2NavVoteCollectorAvailNavVoteCollector24h
        expr: |
          (
            sum(rate(http_requests_total{service="nav-vote-collector",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="nav-vote-collector"}[24h]))
          ) > 0.015000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/nav-vote-collector-avail-nav-vote-collector
          tier: tier-2
        annotations:
          summary: "@domio/nav-vote-collector avail-nav-vote-collector burning budget at >3× over 24h"
          runbook: "runbooks/nav-vote-collector/availability.md"
      - alert: SLOBurnLowT2NavVoteCollectorAvailNavVoteCollector72h
        expr: |
          (
            sum(rate(http_requests_total{service="nav-vote-collector",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="nav-vote-collector"}[72h]))
          ) > 0.005000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/nav-vote-collector-avail-nav-vote-collector
          tier: tier-2
        annotations:
          summary: "@domio/nav-vote-collector avail-nav-vote-collector burning budget at >1× over 72h"
          runbook: "runbooks/nav-vote-collector/availability.md"
- name: slo-burn-rate-@domio/sentiment-collector-avail-sentiment-collector
  rules:
      - alert: SLOBurnHighT3SentimentCollectorAvailSentimentCollector1h
        expr: |
          (
            sum(rate(http_requests_total{service="sentiment-collector",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="sentiment-collector"}[1h]))
          ) > 0.144000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/sentiment-collector-avail-sentiment-collector
          tier: tier-3
        annotations:
          summary: "@domio/sentiment-collector avail-sentiment-collector burning budget at >14.4× over 1h"
          runbook: "runbooks/sentiment-collector/availability.md"
      - alert: SLOBurnMedT3SentimentCollectorAvailSentimentCollector6h
        expr: |
          (
            sum(rate(http_requests_total{service="sentiment-collector",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="sentiment-collector"}[6h]))
          ) > 0.060000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/sentiment-collector-avail-sentiment-collector
          tier: tier-3
        annotations:
          summary: "@domio/sentiment-collector avail-sentiment-collector burning budget at >6× over 6h"
          runbook: "runbooks/sentiment-collector/availability.md"
      - alert: SLOBurnLowT3SentimentCollectorAvailSentimentCollector24h
        expr: |
          (
            sum(rate(http_requests_total{service="sentiment-collector",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="sentiment-collector"}[24h]))
          ) > 0.030000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/sentiment-collector-avail-sentiment-collector
          tier: tier-3
        annotations:
          summary: "@domio/sentiment-collector avail-sentiment-collector burning budget at >3× over 24h"
          runbook: "runbooks/sentiment-collector/availability.md"
      - alert: SLOBurnLowT3SentimentCollectorAvailSentimentCollector72h
        expr: |
          (
            sum(rate(http_requests_total{service="sentiment-collector",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="sentiment-collector"}[72h]))
          ) > 0.010000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/sentiment-collector-avail-sentiment-collector
          tier: tier-3
        annotations:
          summary: "@domio/sentiment-collector avail-sentiment-collector burning budget at >1× over 72h"
          runbook: "runbooks/sentiment-collector/availability.md"
- name: slo-burn-rate-@domio/attendance-logger-avail-attendance-logger
  rules:
      - alert: SLOBurnHighT3AttendanceLoggerAvailAttendanceLogger1h
        expr: |
          (
            sum(rate(http_requests_total{service="attendance-logger",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="attendance-logger"}[1h]))
          ) > 0.144000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/attendance-logger-avail-attendance-logger
          tier: tier-3
        annotations:
          summary: "@domio/attendance-logger avail-attendance-logger burning budget at >14.4× over 1h"
          runbook: "runbooks/attendance-logger/availability.md"
      - alert: SLOBurnMedT3AttendanceLoggerAvailAttendanceLogger6h
        expr: |
          (
            sum(rate(http_requests_total{service="attendance-logger",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="attendance-logger"}[6h]))
          ) > 0.060000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/attendance-logger-avail-attendance-logger
          tier: tier-3
        annotations:
          summary: "@domio/attendance-logger avail-attendance-logger burning budget at >6× over 6h"
          runbook: "runbooks/attendance-logger/availability.md"
      - alert: SLOBurnLowT3AttendanceLoggerAvailAttendanceLogger24h
        expr: |
          (
            sum(rate(http_requests_total{service="attendance-logger",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="attendance-logger"}[24h]))
          ) > 0.030000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/attendance-logger-avail-attendance-logger
          tier: tier-3
        annotations:
          summary: "@domio/attendance-logger avail-attendance-logger burning budget at >3× over 24h"
          runbook: "runbooks/attendance-logger/availability.md"
      - alert: SLOBurnLowT3AttendanceLoggerAvailAttendanceLogger72h
        expr: |
          (
            sum(rate(http_requests_total{service="attendance-logger",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="attendance-logger"}[72h]))
          ) > 0.010000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/attendance-logger-avail-attendance-logger
          tier: tier-3
        annotations:
          summary: "@domio/attendance-logger avail-attendance-logger burning budget at >1× over 72h"
          runbook: "runbooks/attendance-logger/availability.md"
- name: slo-burn-rate-@domio/feedback-collector-avail-feedback-collector
  rules:
      - alert: SLOBurnHighT3FeedbackCollectorAvailFeedbackCollector1h
        expr: |
          (
            sum(rate(http_requests_total{service="feedback-collector",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="feedback-collector"}[1h]))
          ) > 0.144000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/feedback-collector-avail-feedback-collector
          tier: tier-3
        annotations:
          summary: "@domio/feedback-collector avail-feedback-collector burning budget at >14.4× over 1h"
          runbook: "runbooks/feedback-collector/availability.md"
      - alert: SLOBurnMedT3FeedbackCollectorAvailFeedbackCollector6h
        expr: |
          (
            sum(rate(http_requests_total{service="feedback-collector",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="feedback-collector"}[6h]))
          ) > 0.060000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/feedback-collector-avail-feedback-collector
          tier: tier-3
        annotations:
          summary: "@domio/feedback-collector avail-feedback-collector burning budget at >6× over 6h"
          runbook: "runbooks/feedback-collector/availability.md"
      - alert: SLOBurnLowT3FeedbackCollectorAvailFeedbackCollector24h
        expr: |
          (
            sum(rate(http_requests_total{service="feedback-collector",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="feedback-collector"}[24h]))
          ) > 0.030000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/feedback-collector-avail-feedback-collector
          tier: tier-3
        annotations:
          summary: "@domio/feedback-collector avail-feedback-collector burning budget at >3× over 24h"
          runbook: "runbooks/feedback-collector/availability.md"
      - alert: SLOBurnLowT3FeedbackCollectorAvailFeedbackCollector72h
        expr: |
          (
            sum(rate(http_requests_total{service="feedback-collector",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="feedback-collector"}[72h]))
          ) > 0.010000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/feedback-collector-avail-feedback-collector
          tier: tier-3
        annotations:
          summary: "@domio/feedback-collector avail-feedback-collector burning budget at >1× over 72h"
          runbook: "runbooks/feedback-collector/availability.md"
- name: slo-burn-rate-@domio/translation-pipeline-lat-translation-p95
  rules:
      - alert: SLOBurnHighT2TranslationPipelineLatTranslationP951h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="translation-pipeline",le="2"}[1h]))
              /
              sum(rate(http_request_duration_seconds_count{service="translation-pipeline"}[1h]))
            )
          ) > 0.720000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/translation-pipeline-lat-translation-p95
          tier: tier-2
        annotations:
          summary: "@domio/translation-pipeline lat-translation-p95 burning budget at >14.4× over 1h"
          runbook: "runbooks/translation-pipeline/latency.md"
      - alert: SLOBurnMedT2TranslationPipelineLatTranslationP956h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="translation-pipeline",le="2"}[6h]))
              /
              sum(rate(http_request_duration_seconds_count{service="translation-pipeline"}[6h]))
            )
          ) > 0.300000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/translation-pipeline-lat-translation-p95
          tier: tier-2
        annotations:
          summary: "@domio/translation-pipeline lat-translation-p95 burning budget at >6× over 6h"
          runbook: "runbooks/translation-pipeline/latency.md"
      - alert: SLOBurnLowT2TranslationPipelineLatTranslationP9524h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="translation-pipeline",le="2"}[24h]))
              /
              sum(rate(http_request_duration_seconds_count{service="translation-pipeline"}[24h]))
            )
          ) > 0.150000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/translation-pipeline-lat-translation-p95
          tier: tier-2
        annotations:
          summary: "@domio/translation-pipeline lat-translation-p95 burning budget at >3× over 24h"
          runbook: "runbooks/translation-pipeline/latency.md"
      - alert: SLOBurnLowT2TranslationPipelineLatTranslationP9572h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="translation-pipeline",le="2"}[72h]))
              /
              sum(rate(http_request_duration_seconds_count{service="translation-pipeline"}[72h]))
            )
          ) > 0.050000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/translation-pipeline-lat-translation-p95
          tier: tier-2
        annotations:
          summary: "@domio/translation-pipeline lat-translation-p95 burning budget at >1× over 72h"
          runbook: "runbooks/translation-pipeline/latency.md"
- name: slo-burn-rate-@domio/stt-provider-lat-stt-p95
  rules:
      - alert: SLOBurnHighT2SttProviderLatSttP951h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="stt-provider",le="1.5"}[1h]))
              /
              sum(rate(http_request_duration_seconds_count{service="stt-provider"}[1h]))
            )
          ) > 0.720000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/stt-provider-lat-stt-p95
          tier: tier-2
        annotations:
          summary: "@domio/stt-provider lat-stt-p95 burning budget at >14.4× over 1h"
          runbook: "runbooks/stt-provider/latency.md"
      - alert: SLOBurnMedT2SttProviderLatSttP956h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="stt-provider",le="1.5"}[6h]))
              /
              sum(rate(http_request_duration_seconds_count{service="stt-provider"}[6h]))
            )
          ) > 0.300000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/stt-provider-lat-stt-p95
          tier: tier-2
        annotations:
          summary: "@domio/stt-provider lat-stt-p95 burning budget at >6× over 6h"
          runbook: "runbooks/stt-provider/latency.md"
      - alert: SLOBurnLowT2SttProviderLatSttP9524h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="stt-provider",le="1.5"}[24h]))
              /
              sum(rate(http_request_duration_seconds_count{service="stt-provider"}[24h]))
            )
          ) > 0.150000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/stt-provider-lat-stt-p95
          tier: tier-2
        annotations:
          summary: "@domio/stt-provider lat-stt-p95 burning budget at >3× over 24h"
          runbook: "runbooks/stt-provider/latency.md"
      - alert: SLOBurnLowT2SttProviderLatSttP9572h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="stt-provider",le="1.5"}[72h]))
              /
              sum(rate(http_request_duration_seconds_count{service="stt-provider"}[72h]))
            )
          ) > 0.050000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/stt-provider-lat-stt-p95
          tier: tier-2
        annotations:
          summary: "@domio/stt-provider lat-stt-p95 burning budget at >1× over 72h"
          runbook: "runbooks/stt-provider/latency.md"
- name: slo-burn-rate-@domio/mt-provider-lat-mt-p95
  rules:
      - alert: SLOBurnHighT2MtProviderLatMtP951h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="mt-provider",le="1"}[1h]))
              /
              sum(rate(http_request_duration_seconds_count{service="mt-provider"}[1h]))
            )
          ) > 0.720000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/mt-provider-lat-mt-p95
          tier: tier-2
        annotations:
          summary: "@domio/mt-provider lat-mt-p95 burning budget at >14.4× over 1h"
          runbook: "runbooks/mt-provider/latency.md"
      - alert: SLOBurnMedT2MtProviderLatMtP956h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="mt-provider",le="1"}[6h]))
              /
              sum(rate(http_request_duration_seconds_count{service="mt-provider"}[6h]))
            )
          ) > 0.300000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/mt-provider-lat-mt-p95
          tier: tier-2
        annotations:
          summary: "@domio/mt-provider lat-mt-p95 burning budget at >6× over 6h"
          runbook: "runbooks/mt-provider/latency.md"
      - alert: SLOBurnLowT2MtProviderLatMtP9524h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="mt-provider",le="1"}[24h]))
              /
              sum(rate(http_request_duration_seconds_count{service="mt-provider"}[24h]))
            )
          ) > 0.150000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/mt-provider-lat-mt-p95
          tier: tier-2
        annotations:
          summary: "@domio/mt-provider lat-mt-p95 burning budget at >3× over 24h"
          runbook: "runbooks/mt-provider/latency.md"
      - alert: SLOBurnLowT2MtProviderLatMtP9572h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="mt-provider",le="1"}[72h]))
              /
              sum(rate(http_request_duration_seconds_count{service="mt-provider"}[72h]))
            )
          ) > 0.050000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/mt-provider-lat-mt-p95
          tier: tier-2
        annotations:
          summary: "@domio/mt-provider lat-mt-p95 burning budget at >1× over 72h"
          runbook: "runbooks/mt-provider/latency.md"
- name: slo-burn-rate-@domio/tts-provider-lat-tts-p95
  rules:
      - alert: SLOBurnHighT2TtsProviderLatTtsP951h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="tts-provider",le="2"}[1h]))
              /
              sum(rate(http_request_duration_seconds_count{service="tts-provider"}[1h]))
            )
          ) > 0.720000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/tts-provider-lat-tts-p95
          tier: tier-2
        annotations:
          summary: "@domio/tts-provider lat-tts-p95 burning budget at >14.4× over 1h"
          runbook: "runbooks/tts-provider/latency.md"
      - alert: SLOBurnMedT2TtsProviderLatTtsP956h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="tts-provider",le="2"}[6h]))
              /
              sum(rate(http_request_duration_seconds_count{service="tts-provider"}[6h]))
            )
          ) > 0.300000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/tts-provider-lat-tts-p95
          tier: tier-2
        annotations:
          summary: "@domio/tts-provider lat-tts-p95 burning budget at >6× over 6h"
          runbook: "runbooks/tts-provider/latency.md"
      - alert: SLOBurnLowT2TtsProviderLatTtsP9524h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="tts-provider",le="2"}[24h]))
              /
              sum(rate(http_request_duration_seconds_count{service="tts-provider"}[24h]))
            )
          ) > 0.150000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/tts-provider-lat-tts-p95
          tier: tier-2
        annotations:
          summary: "@domio/tts-provider lat-tts-p95 burning budget at >3× over 24h"
          runbook: "runbooks/tts-provider/latency.md"
      - alert: SLOBurnLowT2TtsProviderLatTtsP9572h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="tts-provider",le="2"}[72h]))
              /
              sum(rate(http_request_duration_seconds_count{service="tts-provider"}[72h]))
            )
          ) > 0.050000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/tts-provider-lat-tts-p95
          tier: tier-2
        annotations:
          summary: "@domio/tts-provider lat-tts-p95 burning budget at >1× over 72h"
          runbook: "runbooks/tts-provider/latency.md"
- name: slo-burn-rate-@domio/moderation-blocklist-avail-moderation-blocklist
  rules:
      - alert: SLOBurnHighT1ModerationBlocklistAvailModerationBlocklist1h
        expr: |
          (
            sum(rate(http_requests_total{service="moderation-blocklist",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="moderation-blocklist"}[1h]))
          ) > 0.014400
        for: 2m
        labels:
          severity: page
          slo: @domio/moderation-blocklist-avail-moderation-blocklist
          tier: tier-1
        annotations:
          summary: "@domio/moderation-blocklist avail-moderation-blocklist burning budget at >14.4× over 1h"
          runbook: "runbooks/moderation-blocklist/availability.md"
      - alert: SLOBurnMedT1ModerationBlocklistAvailModerationBlocklist6h
        expr: |
          (
            sum(rate(http_requests_total{service="moderation-blocklist",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="moderation-blocklist"}[6h]))
          ) > 0.006000
        for: 5m
        labels:
          severity: page
          slo: @domio/moderation-blocklist-avail-moderation-blocklist
          tier: tier-1
        annotations:
          summary: "@domio/moderation-blocklist avail-moderation-blocklist burning budget at >6× over 6h"
          runbook: "runbooks/moderation-blocklist/availability.md"
      - alert: SLOBurnLowT1ModerationBlocklistAvailModerationBlocklist24h
        expr: |
          (
            sum(rate(http_requests_total{service="moderation-blocklist",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="moderation-blocklist"}[24h]))
          ) > 0.003000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/moderation-blocklist-avail-moderation-blocklist
          tier: tier-1
        annotations:
          summary: "@domio/moderation-blocklist avail-moderation-blocklist burning budget at >3× over 24h"
          runbook: "runbooks/moderation-blocklist/availability.md"
      - alert: SLOBurnLowT1ModerationBlocklistAvailModerationBlocklist72h
        expr: |
          (
            sum(rate(http_requests_total{service="moderation-blocklist",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="moderation-blocklist"}[72h]))
          ) > 0.001000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/moderation-blocklist-avail-moderation-blocklist
          tier: tier-1
        annotations:
          summary: "@domio/moderation-blocklist avail-moderation-blocklist burning budget at >1× over 72h"
          runbook: "runbooks/moderation-blocklist/availability.md"
- name: slo-burn-rate-@domio/moderation-ml-lat-moderation-ml-p95
  rules:
      - alert: SLOBurnHighT2ModerationMlLatModerationMlP951h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="moderation-ml",le="0.8"}[1h]))
              /
              sum(rate(http_request_duration_seconds_count{service="moderation-ml"}[1h]))
            )
          ) > 0.720000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/moderation-ml-lat-moderation-ml-p95
          tier: tier-2
        annotations:
          summary: "@domio/moderation-ml lat-moderation-ml-p95 burning budget at >14.4× over 1h"
          runbook: "runbooks/moderation-ml/latency.md"
      - alert: SLOBurnMedT2ModerationMlLatModerationMlP956h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="moderation-ml",le="0.8"}[6h]))
              /
              sum(rate(http_request_duration_seconds_count{service="moderation-ml"}[6h]))
            )
          ) > 0.300000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/moderation-ml-lat-moderation-ml-p95
          tier: tier-2
        annotations:
          summary: "@domio/moderation-ml lat-moderation-ml-p95 burning budget at >6× over 6h"
          runbook: "runbooks/moderation-ml/latency.md"
      - alert: SLOBurnLowT2ModerationMlLatModerationMlP9524h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="moderation-ml",le="0.8"}[24h]))
              /
              sum(rate(http_request_duration_seconds_count{service="moderation-ml"}[24h]))
            )
          ) > 0.150000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/moderation-ml-lat-moderation-ml-p95
          tier: tier-2
        annotations:
          summary: "@domio/moderation-ml lat-moderation-ml-p95 burning budget at >3× over 24h"
          runbook: "runbooks/moderation-ml/latency.md"
      - alert: SLOBurnLowT2ModerationMlLatModerationMlP9572h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="moderation-ml",le="0.8"}[72h]))
              /
              sum(rate(http_request_duration_seconds_count{service="moderation-ml"}[72h]))
            )
          ) > 0.050000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/moderation-ml-lat-moderation-ml-p95
          tier: tier-2
        annotations:
          summary: "@domio/moderation-ml lat-moderation-ml-p95 burning budget at >1× over 72h"
          runbook: "runbooks/moderation-ml/latency.md"
- name: slo-burn-rate-@domio/guests-service-avail-guests
  rules:
      - alert: SLOBurnHighT2GuestsAvailGuests1h
        expr: |
          (
            sum(rate(http_requests_total{service="guests",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="guests"}[1h]))
          ) > 0.072000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/guests-service-avail-guests
          tier: tier-2
        annotations:
          summary: "@domio/guests-service avail-guests burning budget at >14.4× over 1h"
          runbook: "runbooks/guests-service/availability.md"
      - alert: SLOBurnMedT2GuestsAvailGuests6h
        expr: |
          (
            sum(rate(http_requests_total{service="guests",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="guests"}[6h]))
          ) > 0.030000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/guests-service-avail-guests
          tier: tier-2
        annotations:
          summary: "@domio/guests-service avail-guests burning budget at >6× over 6h"
          runbook: "runbooks/guests-service/availability.md"
      - alert: SLOBurnLowT2GuestsAvailGuests24h
        expr: |
          (
            sum(rate(http_requests_total{service="guests",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="guests"}[24h]))
          ) > 0.015000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/guests-service-avail-guests
          tier: tier-2
        annotations:
          summary: "@domio/guests-service avail-guests burning budget at >3× over 24h"
          runbook: "runbooks/guests-service/availability.md"
      - alert: SLOBurnLowT2GuestsAvailGuests72h
        expr: |
          (
            sum(rate(http_requests_total{service="guests",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="guests"}[72h]))
          ) > 0.005000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/guests-service-avail-guests
          tier: tier-2
        annotations:
          summary: "@domio/guests-service avail-guests burning budget at >1× over 72h"
          runbook: "runbooks/guests-service/availability.md"
- name: slo-burn-rate-@domio/phone-pairing-avail-phone-pairing
  rules:
      - alert: SLOBurnHighT2PhonePairingAvailPhonePairing1h
        expr: |
          (
            sum(rate(http_requests_total{service="phone-pairing",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="phone-pairing"}[1h]))
          ) > 0.072000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/phone-pairing-avail-phone-pairing
          tier: tier-2
        annotations:
          summary: "@domio/phone-pairing avail-phone-pairing burning budget at >14.4× over 1h"
          runbook: "runbooks/phone-pairing/availability.md"
      - alert: SLOBurnMedT2PhonePairingAvailPhonePairing6h
        expr: |
          (
            sum(rate(http_requests_total{service="phone-pairing",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="phone-pairing"}[6h]))
          ) > 0.030000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/phone-pairing-avail-phone-pairing
          tier: tier-2
        annotations:
          summary: "@domio/phone-pairing avail-phone-pairing burning budget at >6× over 6h"
          runbook: "runbooks/phone-pairing/availability.md"
      - alert: SLOBurnLowT2PhonePairingAvailPhonePairing24h
        expr: |
          (
            sum(rate(http_requests_total{service="phone-pairing",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="phone-pairing"}[24h]))
          ) > 0.015000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/phone-pairing-avail-phone-pairing
          tier: tier-2
        annotations:
          summary: "@domio/phone-pairing avail-phone-pairing burning budget at >3× over 24h"
          runbook: "runbooks/phone-pairing/availability.md"
      - alert: SLOBurnLowT2PhonePairingAvailPhonePairing72h
        expr: |
          (
            sum(rate(http_requests_total{service="phone-pairing",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="phone-pairing"}[72h]))
          ) > 0.005000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/phone-pairing-avail-phone-pairing
          tier: tier-2
        annotations:
          summary: "@domio/phone-pairing avail-phone-pairing burning budget at >1× over 72h"
          runbook: "runbooks/phone-pairing/availability.md"
- name: slo-burn-rate-@domio/thumbnail-avail-thumbnail
  rules:
      - alert: SLOBurnHighT3ThumbnailAvailThumbnail1h
        expr: |
          (
            sum(rate(http_requests_total{service="thumbnail",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="thumbnail"}[1h]))
          ) > 0.144000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/thumbnail-avail-thumbnail
          tier: tier-3
        annotations:
          summary: "@domio/thumbnail avail-thumbnail burning budget at >14.4× over 1h"
          runbook: "runbooks/thumbnail/availability.md"
      - alert: SLOBurnMedT3ThumbnailAvailThumbnail6h
        expr: |
          (
            sum(rate(http_requests_total{service="thumbnail",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="thumbnail"}[6h]))
          ) > 0.060000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/thumbnail-avail-thumbnail
          tier: tier-3
        annotations:
          summary: "@domio/thumbnail avail-thumbnail burning budget at >6× over 6h"
          runbook: "runbooks/thumbnail/availability.md"
      - alert: SLOBurnLowT3ThumbnailAvailThumbnail24h
        expr: |
          (
            sum(rate(http_requests_total{service="thumbnail",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="thumbnail"}[24h]))
          ) > 0.030000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/thumbnail-avail-thumbnail
          tier: tier-3
        annotations:
          summary: "@domio/thumbnail avail-thumbnail burning budget at >3× over 24h"
          runbook: "runbooks/thumbnail/availability.md"
      - alert: SLOBurnLowT3ThumbnailAvailThumbnail72h
        expr: |
          (
            sum(rate(http_requests_total{service="thumbnail",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="thumbnail"}[72h]))
          ) > 0.010000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/thumbnail-avail-thumbnail
          tier: tier-3
        annotations:
          summary: "@domio/thumbnail avail-thumbnail burning budget at >1× over 72h"
          runbook: "runbooks/thumbnail/availability.md"
- name: slo-burn-rate-@domio/export-pipeline-lat-export-p95
  rules:
      - alert: SLOBurnHighT2ExportPipelineLatExportP951h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="export-pipeline",le="30"}[1h]))
              /
              sum(rate(http_request_duration_seconds_count{service="export-pipeline"}[1h]))
            )
          ) > 0.720000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/export-pipeline-lat-export-p95
          tier: tier-2
        annotations:
          summary: "@domio/export-pipeline lat-export-p95 burning budget at >14.4× over 1h"
          runbook: "runbooks/export-pipeline/latency.md"
      - alert: SLOBurnMedT2ExportPipelineLatExportP956h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="export-pipeline",le="30"}[6h]))
              /
              sum(rate(http_request_duration_seconds_count{service="export-pipeline"}[6h]))
            )
          ) > 0.300000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/export-pipeline-lat-export-p95
          tier: tier-2
        annotations:
          summary: "@domio/export-pipeline lat-export-p95 burning budget at >6× over 6h"
          runbook: "runbooks/export-pipeline/latency.md"
      - alert: SLOBurnLowT2ExportPipelineLatExportP9524h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="export-pipeline",le="30"}[24h]))
              /
              sum(rate(http_request_duration_seconds_count{service="export-pipeline"}[24h]))
            )
          ) > 0.150000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/export-pipeline-lat-export-p95
          tier: tier-2
        annotations:
          summary: "@domio/export-pipeline lat-export-p95 burning budget at >3× over 24h"
          runbook: "runbooks/export-pipeline/latency.md"
      - alert: SLOBurnLowT2ExportPipelineLatExportP9572h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="export-pipeline",le="30"}[72h]))
              /
              sum(rate(http_request_duration_seconds_count{service="export-pipeline"}[72h]))
            )
          ) > 0.050000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/export-pipeline-lat-export-p95
          tier: tier-2
        annotations:
          summary: "@domio/export-pipeline lat-export-p95 burning budget at >1× over 72h"
          runbook: "runbooks/export-pipeline/latency.md"
- name: slo-burn-rate-@domio/video-pipeline-lat-video-pipeline-p95
  rules:
      - alert: SLOBurnHighT2VideoPipelineLatVideoPipelineP951h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="video-pipeline",le="60"}[1h]))
              /
              sum(rate(http_request_duration_seconds_count{service="video-pipeline"}[1h]))
            )
          ) > 0.720000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/video-pipeline-lat-video-pipeline-p95
          tier: tier-2
        annotations:
          summary: "@domio/video-pipeline lat-video-pipeline-p95 burning budget at >14.4× over 1h"
          runbook: "runbooks/video-pipeline/latency.md"
      - alert: SLOBurnMedT2VideoPipelineLatVideoPipelineP956h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="video-pipeline",le="60"}[6h]))
              /
              sum(rate(http_request_duration_seconds_count{service="video-pipeline"}[6h]))
            )
          ) > 0.300000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/video-pipeline-lat-video-pipeline-p95
          tier: tier-2
        annotations:
          summary: "@domio/video-pipeline lat-video-pipeline-p95 burning budget at >6× over 6h"
          runbook: "runbooks/video-pipeline/latency.md"
      - alert: SLOBurnLowT2VideoPipelineLatVideoPipelineP9524h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="video-pipeline",le="60"}[24h]))
              /
              sum(rate(http_request_duration_seconds_count{service="video-pipeline"}[24h]))
            )
          ) > 0.150000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/video-pipeline-lat-video-pipeline-p95
          tier: tier-2
        annotations:
          summary: "@domio/video-pipeline lat-video-pipeline-p95 burning budget at >3× over 24h"
          runbook: "runbooks/video-pipeline/latency.md"
      - alert: SLOBurnLowT2VideoPipelineLatVideoPipelineP9572h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="video-pipeline",le="60"}[72h]))
              /
              sum(rate(http_request_duration_seconds_count{service="video-pipeline"}[72h]))
            )
          ) > 0.050000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/video-pipeline-lat-video-pipeline-p95
          tier: tier-2
        annotations:
          summary: "@domio/video-pipeline lat-video-pipeline-p95 burning budget at >1× over 72h"
          runbook: "runbooks/video-pipeline/latency.md"
- name: slo-burn-rate-@domio/keyframes-svc-avail-keyframes
  rules:
      - alert: SLOBurnHighT3KeyframesSvcAvailKeyframes1h
        expr: |
          (
            sum(rate(http_requests_total{service="keyframes-svc",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="keyframes-svc"}[1h]))
          ) > 0.144000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/keyframes-svc-avail-keyframes
          tier: tier-3
        annotations:
          summary: "@domio/keyframes-svc avail-keyframes burning budget at >14.4× over 1h"
          runbook: "runbooks/keyframes-svc/availability.md"
      - alert: SLOBurnMedT3KeyframesSvcAvailKeyframes6h
        expr: |
          (
            sum(rate(http_requests_total{service="keyframes-svc",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="keyframes-svc"}[6h]))
          ) > 0.060000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/keyframes-svc-avail-keyframes
          tier: tier-3
        annotations:
          summary: "@domio/keyframes-svc avail-keyframes burning budget at >6× over 6h"
          runbook: "runbooks/keyframes-svc/availability.md"
      - alert: SLOBurnLowT3KeyframesSvcAvailKeyframes24h
        expr: |
          (
            sum(rate(http_requests_total{service="keyframes-svc",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="keyframes-svc"}[24h]))
          ) > 0.030000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/keyframes-svc-avail-keyframes
          tier: tier-3
        annotations:
          summary: "@domio/keyframes-svc avail-keyframes burning budget at >3× over 24h"
          runbook: "runbooks/keyframes-svc/availability.md"
      - alert: SLOBurnLowT3KeyframesSvcAvailKeyframes72h
        expr: |
          (
            sum(rate(http_requests_total{service="keyframes-svc",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="keyframes-svc"}[72h]))
          ) > 0.010000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/keyframes-svc-avail-keyframes
          tier: tier-3
        annotations:
          summary: "@domio/keyframes-svc avail-keyframes burning budget at >1× over 72h"
          runbook: "runbooks/keyframes-svc/availability.md"
- name: slo-burn-rate-@domio/latex-render-lat-latex-render-p95
  rules:
      - alert: SLOBurnHighT2LatexRenderLatLatexRenderP951h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="latex-render",le="0.5"}[1h]))
              /
              sum(rate(http_request_duration_seconds_count{service="latex-render"}[1h]))
            )
          ) > 0.720000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/latex-render-lat-latex-render-p95
          tier: tier-2
        annotations:
          summary: "@domio/latex-render lat-latex-render-p95 burning budget at >14.4× over 1h"
          runbook: "runbooks/latex-render/latency.md"
      - alert: SLOBurnMedT2LatexRenderLatLatexRenderP956h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="latex-render",le="0.5"}[6h]))
              /
              sum(rate(http_request_duration_seconds_count{service="latex-render"}[6h]))
            )
          ) > 0.300000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/latex-render-lat-latex-render-p95
          tier: tier-2
        annotations:
          summary: "@domio/latex-render lat-latex-render-p95 burning budget at >6× over 6h"
          runbook: "runbooks/latex-render/latency.md"
      - alert: SLOBurnLowT2LatexRenderLatLatexRenderP9524h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="latex-render",le="0.5"}[24h]))
              /
              sum(rate(http_request_duration_seconds_count{service="latex-render"}[24h]))
            )
          ) > 0.150000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/latex-render-lat-latex-render-p95
          tier: tier-2
        annotations:
          summary: "@domio/latex-render lat-latex-render-p95 burning budget at >3× over 24h"
          runbook: "runbooks/latex-render/latency.md"
      - alert: SLOBurnLowT2LatexRenderLatLatexRenderP9572h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="latex-render",le="0.5"}[72h]))
              /
              sum(rate(http_request_duration_seconds_count{service="latex-render"}[72h]))
            )
          ) > 0.050000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/latex-render-lat-latex-render-p95
          tier: tier-2
        annotations:
          summary: "@domio/latex-render lat-latex-render-p95 burning budget at >1× over 72h"
          runbook: "runbooks/latex-render/latency.md"
- name: slo-burn-rate-@domio/shader-registry-avail-shader-registry
  rules:
      - alert: SLOBurnHighT3ShaderRegistryAvailShaderRegistry1h
        expr: |
          (
            sum(rate(http_requests_total{service="shader-registry",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="shader-registry"}[1h]))
          ) > 0.144000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/shader-registry-avail-shader-registry
          tier: tier-3
        annotations:
          summary: "@domio/shader-registry avail-shader-registry burning budget at >14.4× over 1h"
          runbook: "runbooks/shader-registry/availability.md"
      - alert: SLOBurnMedT3ShaderRegistryAvailShaderRegistry6h
        expr: |
          (
            sum(rate(http_requests_total{service="shader-registry",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="shader-registry"}[6h]))
          ) > 0.060000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/shader-registry-avail-shader-registry
          tier: tier-3
        annotations:
          summary: "@domio/shader-registry avail-shader-registry burning budget at >6× over 6h"
          runbook: "runbooks/shader-registry/availability.md"
      - alert: SLOBurnLowT3ShaderRegistryAvailShaderRegistry24h
        expr: |
          (
            sum(rate(http_requests_total{service="shader-registry",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="shader-registry"}[24h]))
          ) > 0.030000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/shader-registry-avail-shader-registry
          tier: tier-3
        annotations:
          summary: "@domio/shader-registry avail-shader-registry burning budget at >3× over 24h"
          runbook: "runbooks/shader-registry/availability.md"
      - alert: SLOBurnLowT3ShaderRegistryAvailShaderRegistry72h
        expr: |
          (
            sum(rate(http_requests_total{service="shader-registry",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="shader-registry"}[72h]))
          ) > 0.010000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/shader-registry-avail-shader-registry
          tier: tier-3
        annotations:
          summary: "@domio/shader-registry avail-shader-registry burning budget at >1× over 72h"
          runbook: "runbooks/shader-registry/availability.md"
- name: slo-burn-rate-@domio/code-sandbox-lat-code-sandbox-p95
  rules:
      - alert: SLOBurnHighT2CodeSandboxLatCodeSandboxP951h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="code-sandbox",le="2"}[1h]))
              /
              sum(rate(http_request_duration_seconds_count{service="code-sandbox"}[1h]))
            )
          ) > 0.720000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/code-sandbox-lat-code-sandbox-p95
          tier: tier-2
        annotations:
          summary: "@domio/code-sandbox lat-code-sandbox-p95 burning budget at >14.4× over 1h"
          runbook: "runbooks/code-sandbox/latency.md"
      - alert: SLOBurnMedT2CodeSandboxLatCodeSandboxP956h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="code-sandbox",le="2"}[6h]))
              /
              sum(rate(http_request_duration_seconds_count{service="code-sandbox"}[6h]))
            )
          ) > 0.300000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/code-sandbox-lat-code-sandbox-p95
          tier: tier-2
        annotations:
          summary: "@domio/code-sandbox lat-code-sandbox-p95 burning budget at >6× over 6h"
          runbook: "runbooks/code-sandbox/latency.md"
      - alert: SLOBurnLowT2CodeSandboxLatCodeSandboxP9524h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="code-sandbox",le="2"}[24h]))
              /
              sum(rate(http_request_duration_seconds_count{service="code-sandbox"}[24h]))
            )
          ) > 0.150000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/code-sandbox-lat-code-sandbox-p95
          tier: tier-2
        annotations:
          summary: "@domio/code-sandbox lat-code-sandbox-p95 burning budget at >3× over 24h"
          runbook: "runbooks/code-sandbox/latency.md"
      - alert: SLOBurnLowT2CodeSandboxLatCodeSandboxP9572h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="code-sandbox",le="2"}[72h]))
              /
              sum(rate(http_request_duration_seconds_count{service="code-sandbox"}[72h]))
            )
          ) > 0.050000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/code-sandbox-lat-code-sandbox-p95
          tier: tier-2
        annotations:
          summary: "@domio/code-sandbox lat-code-sandbox-p95 burning budget at >1× over 72h"
          runbook: "runbooks/code-sandbox/latency.md"
- name: slo-burn-rate-@domio/cad-jobs-avail-cad-jobs
  rules:
      - alert: SLOBurnHighT3CadJobsAvailCadJobs1h
        expr: |
          (
            sum(rate(http_requests_total{service="cad-jobs",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="cad-jobs"}[1h]))
          ) > 0.144000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/cad-jobs-avail-cad-jobs
          tier: tier-3
        annotations:
          summary: "@domio/cad-jobs avail-cad-jobs burning budget at >14.4× over 1h"
          runbook: "runbooks/cad-jobs/availability.md"
      - alert: SLOBurnMedT3CadJobsAvailCadJobs6h
        expr: |
          (
            sum(rate(http_requests_total{service="cad-jobs",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="cad-jobs"}[6h]))
          ) > 0.060000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/cad-jobs-avail-cad-jobs
          tier: tier-3
        annotations:
          summary: "@domio/cad-jobs avail-cad-jobs burning budget at >6× over 6h"
          runbook: "runbooks/cad-jobs/availability.md"
      - alert: SLOBurnLowT3CadJobsAvailCadJobs24h
        expr: |
          (
            sum(rate(http_requests_total{service="cad-jobs",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="cad-jobs"}[24h]))
          ) > 0.030000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/cad-jobs-avail-cad-jobs
          tier: tier-3
        annotations:
          summary: "@domio/cad-jobs avail-cad-jobs burning budget at >3× over 24h"
          runbook: "runbooks/cad-jobs/availability.md"
      - alert: SLOBurnLowT3CadJobsAvailCadJobs72h
        expr: |
          (
            sum(rate(http_requests_total{service="cad-jobs",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="cad-jobs"}[72h]))
          ) > 0.010000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/cad-jobs-avail-cad-jobs
          tier: tier-3
        annotations:
          summary: "@domio/cad-jobs avail-cad-jobs burning budget at >1× over 72h"
          runbook: "runbooks/cad-jobs/availability.md"
- name: slo-burn-rate-@domio/ar-sessions-lat-ar-sessions-p95
  rules:
      - alert: SLOBurnHighT2ArSessionsLatArSessionsP951h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="ar-sessions",le="0.5"}[1h]))
              /
              sum(rate(http_request_duration_seconds_count{service="ar-sessions"}[1h]))
            )
          ) > 0.720000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/ar-sessions-lat-ar-sessions-p95
          tier: tier-2
        annotations:
          summary: "@domio/ar-sessions lat-ar-sessions-p95 burning budget at >14.4× over 1h"
          runbook: "runbooks/ar-sessions/latency.md"
      - alert: SLOBurnMedT2ArSessionsLatArSessionsP956h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="ar-sessions",le="0.5"}[6h]))
              /
              sum(rate(http_request_duration_seconds_count{service="ar-sessions"}[6h]))
            )
          ) > 0.300000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/ar-sessions-lat-ar-sessions-p95
          tier: tier-2
        annotations:
          summary: "@domio/ar-sessions lat-ar-sessions-p95 burning budget at >6× over 6h"
          runbook: "runbooks/ar-sessions/latency.md"
      - alert: SLOBurnLowT2ArSessionsLatArSessionsP9524h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="ar-sessions",le="0.5"}[24h]))
              /
              sum(rate(http_request_duration_seconds_count{service="ar-sessions"}[24h]))
            )
          ) > 0.150000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/ar-sessions-lat-ar-sessions-p95
          tier: tier-2
        annotations:
          summary: "@domio/ar-sessions lat-ar-sessions-p95 burning budget at >3× over 24h"
          runbook: "runbooks/ar-sessions/latency.md"
      - alert: SLOBurnLowT2ArSessionsLatArSessionsP9572h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="ar-sessions",le="0.5"}[72h]))
              /
              sum(rate(http_request_duration_seconds_count{service="ar-sessions"}[72h]))
            )
          ) > 0.050000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/ar-sessions-lat-ar-sessions-p95
          tier: tier-2
        annotations:
          summary: "@domio/ar-sessions lat-ar-sessions-p95 burning budget at >1× over 72h"
          runbook: "runbooks/ar-sessions/latency.md"
- name: slo-burn-rate-@domio/asset-api-lat-asset-api-p95
  rules:
      - alert: SLOBurnHighT2AssetApiLatAssetApiP951h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="asset-api",le="0.3"}[1h]))
              /
              sum(rate(http_request_duration_seconds_count{service="asset-api"}[1h]))
            )
          ) > 0.720000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/asset-api-lat-asset-api-p95
          tier: tier-2
        annotations:
          summary: "@domio/asset-api lat-asset-api-p95 burning budget at >14.4× over 1h"
          runbook: "runbooks/asset-api/latency.md"
      - alert: SLOBurnMedT2AssetApiLatAssetApiP956h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="asset-api",le="0.3"}[6h]))
              /
              sum(rate(http_request_duration_seconds_count{service="asset-api"}[6h]))
            )
          ) > 0.300000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/asset-api-lat-asset-api-p95
          tier: tier-2
        annotations:
          summary: "@domio/asset-api lat-asset-api-p95 burning budget at >6× over 6h"
          runbook: "runbooks/asset-api/latency.md"
      - alert: SLOBurnLowT2AssetApiLatAssetApiP9524h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="asset-api",le="0.3"}[24h]))
              /
              sum(rate(http_request_duration_seconds_count{service="asset-api"}[24h]))
            )
          ) > 0.150000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/asset-api-lat-asset-api-p95
          tier: tier-2
        annotations:
          summary: "@domio/asset-api lat-asset-api-p95 burning budget at >3× over 24h"
          runbook: "runbooks/asset-api/latency.md"
      - alert: SLOBurnLowT2AssetApiLatAssetApiP9572h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="asset-api",le="0.3"}[72h]))
              /
              sum(rate(http_request_duration_seconds_count{service="asset-api"}[72h]))
            )
          ) > 0.050000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/asset-api-lat-asset-api-p95
          tier: tier-2
        annotations:
          summary: "@domio/asset-api lat-asset-api-p95 burning budget at >1× over 72h"
          runbook: "runbooks/asset-api/latency.md"
- name: slo-burn-rate-@domio/embed-proxy-lat-embed-proxy-p95
  rules:
      - alert: SLOBurnHighT2EmbedProxyLatEmbedProxyP951h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="embed-proxy",le="0.4"}[1h]))
              /
              sum(rate(http_request_duration_seconds_count{service="embed-proxy"}[1h]))
            )
          ) > 0.720000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/embed-proxy-lat-embed-proxy-p95
          tier: tier-2
        annotations:
          summary: "@domio/embed-proxy lat-embed-proxy-p95 burning budget at >14.4× over 1h"
          runbook: "runbooks/embed-proxy/latency.md"
      - alert: SLOBurnMedT2EmbedProxyLatEmbedProxyP956h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="embed-proxy",le="0.4"}[6h]))
              /
              sum(rate(http_request_duration_seconds_count{service="embed-proxy"}[6h]))
            )
          ) > 0.300000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/embed-proxy-lat-embed-proxy-p95
          tier: tier-2
        annotations:
          summary: "@domio/embed-proxy lat-embed-proxy-p95 burning budget at >6× over 6h"
          runbook: "runbooks/embed-proxy/latency.md"
      - alert: SLOBurnLowT2EmbedProxyLatEmbedProxyP9524h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="embed-proxy",le="0.4"}[24h]))
              /
              sum(rate(http_request_duration_seconds_count{service="embed-proxy"}[24h]))
            )
          ) > 0.150000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/embed-proxy-lat-embed-proxy-p95
          tier: tier-2
        annotations:
          summary: "@domio/embed-proxy lat-embed-proxy-p95 burning budget at >3× over 24h"
          runbook: "runbooks/embed-proxy/latency.md"
      - alert: SLOBurnLowT2EmbedProxyLatEmbedProxyP9572h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="embed-proxy",le="0.4"}[72h]))
              /
              sum(rate(http_request_duration_seconds_count{service="embed-proxy"}[72h]))
            )
          ) > 0.050000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/embed-proxy-lat-embed-proxy-p95
          tier: tier-2
        annotations:
          summary: "@domio/embed-proxy lat-embed-proxy-p95 burning budget at >1× over 72h"
          runbook: "runbooks/embed-proxy/latency.md"
- name: slo-burn-rate-@domio/magic-move-lat-magic-move-p95
  rules:
      - alert: SLOBurnHighT2MagicMoveLatMagicMoveP951h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="magic-move",le="0.8"}[1h]))
              /
              sum(rate(http_request_duration_seconds_count{service="magic-move"}[1h]))
            )
          ) > 0.720000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/magic-move-lat-magic-move-p95
          tier: tier-2
        annotations:
          summary: "@domio/magic-move lat-magic-move-p95 burning budget at >14.4× over 1h"
          runbook: "runbooks/magic-move/latency.md"
      - alert: SLOBurnMedT2MagicMoveLatMagicMoveP956h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="magic-move",le="0.8"}[6h]))
              /
              sum(rate(http_request_duration_seconds_count{service="magic-move"}[6h]))
            )
          ) > 0.300000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/magic-move-lat-magic-move-p95
          tier: tier-2
        annotations:
          summary: "@domio/magic-move lat-magic-move-p95 burning budget at >6× over 6h"
          runbook: "runbooks/magic-move/latency.md"
      - alert: SLOBurnLowT2MagicMoveLatMagicMoveP9524h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="magic-move",le="0.8"}[24h]))
              /
              sum(rate(http_request_duration_seconds_count{service="magic-move"}[24h]))
            )
          ) > 0.150000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/magic-move-lat-magic-move-p95
          tier: tier-2
        annotations:
          summary: "@domio/magic-move lat-magic-move-p95 burning budget at >3× over 24h"
          runbook: "runbooks/magic-move/latency.md"
      - alert: SLOBurnLowT2MagicMoveLatMagicMoveP9572h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="magic-move",le="0.8"}[72h]))
              /
              sum(rate(http_request_duration_seconds_count{service="magic-move"}[72h]))
            )
          ) > 0.050000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/magic-move-lat-magic-move-p95
          tier: tier-2
        annotations:
          summary: "@domio/magic-move lat-magic-move-p95 burning budget at >1× over 72h"
          runbook: "runbooks/magic-move/latency.md"
- name: slo-burn-rate-@domio/prototype-runtime-service-lat-prototype-runtime-p95
  rules:
      - alert: SLOBurnHighT2PrototypeRuntimeLatPrototypeRuntimeP951h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="prototype-runtime",le="0.1"}[1h]))
              /
              sum(rate(http_request_duration_seconds_count{service="prototype-runtime"}[1h]))
            )
          ) > 0.720000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/prototype-runtime-service-lat-prototype-runtime-p95
          tier: tier-2
        annotations:
          summary: "@domio/prototype-runtime-service lat-prototype-runtime-p95 burning budget at >14.4× over 1h"
          runbook: "runbooks/prototype-runtime-service/latency.md"
      - alert: SLOBurnMedT2PrototypeRuntimeLatPrototypeRuntimeP956h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="prototype-runtime",le="0.1"}[6h]))
              /
              sum(rate(http_request_duration_seconds_count{service="prototype-runtime"}[6h]))
            )
          ) > 0.300000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/prototype-runtime-service-lat-prototype-runtime-p95
          tier: tier-2
        annotations:
          summary: "@domio/prototype-runtime-service lat-prototype-runtime-p95 burning budget at >6× over 6h"
          runbook: "runbooks/prototype-runtime-service/latency.md"
      - alert: SLOBurnLowT2PrototypeRuntimeLatPrototypeRuntimeP9524h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="prototype-runtime",le="0.1"}[24h]))
              /
              sum(rate(http_request_duration_seconds_count{service="prototype-runtime"}[24h]))
            )
          ) > 0.150000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/prototype-runtime-service-lat-prototype-runtime-p95
          tier: tier-2
        annotations:
          summary: "@domio/prototype-runtime-service lat-prototype-runtime-p95 burning budget at >3× over 24h"
          runbook: "runbooks/prototype-runtime-service/latency.md"
      - alert: SLOBurnLowT2PrototypeRuntimeLatPrototypeRuntimeP9572h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="prototype-runtime",le="0.1"}[72h]))
              /
              sum(rate(http_request_duration_seconds_count{service="prototype-runtime"}[72h]))
            )
          ) > 0.050000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/prototype-runtime-service-lat-prototype-runtime-p95
          tier: tier-2
        annotations:
          summary: "@domio/prototype-runtime-service lat-prototype-runtime-p95 burning budget at >1× over 72h"
          runbook: "runbooks/prototype-runtime-service/latency.md"
- name: slo-burn-rate-@domio/prototype-recorder-service-avail-prototype-recorder
  rules:
      - alert: SLOBurnHighT3PrototypeRecorderAvailPrototypeRecorder1h
        expr: |
          (
            sum(rate(http_requests_total{service="prototype-recorder",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="prototype-recorder"}[1h]))
          ) > 0.144000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/prototype-recorder-service-avail-prototype-recorder
          tier: tier-3
        annotations:
          summary: "@domio/prototype-recorder-service avail-prototype-recorder burning budget at >14.4× over 1h"
          runbook: "runbooks/prototype-recorder-service/availability.md"
      - alert: SLOBurnMedT3PrototypeRecorderAvailPrototypeRecorder6h
        expr: |
          (
            sum(rate(http_requests_total{service="prototype-recorder",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="prototype-recorder"}[6h]))
          ) > 0.060000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/prototype-recorder-service-avail-prototype-recorder
          tier: tier-3
        annotations:
          summary: "@domio/prototype-recorder-service avail-prototype-recorder burning budget at >6× over 6h"
          runbook: "runbooks/prototype-recorder-service/availability.md"
      - alert: SLOBurnLowT3PrototypeRecorderAvailPrototypeRecorder24h
        expr: |
          (
            sum(rate(http_requests_total{service="prototype-recorder",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="prototype-recorder"}[24h]))
          ) > 0.030000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/prototype-recorder-service-avail-prototype-recorder
          tier: tier-3
        annotations:
          summary: "@domio/prototype-recorder-service avail-prototype-recorder burning budget at >3× over 24h"
          runbook: "runbooks/prototype-recorder-service/availability.md"
      - alert: SLOBurnLowT3PrototypeRecorderAvailPrototypeRecorder72h
        expr: |
          (
            sum(rate(http_requests_total{service="prototype-recorder",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="prototype-recorder"}[72h]))
          ) > 0.010000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/prototype-recorder-service-avail-prototype-recorder
          tier: tier-3
        annotations:
          summary: "@domio/prototype-recorder-service avail-prototype-recorder burning budget at >1× over 72h"
          runbook: "runbooks/prototype-recorder-service/availability.md"
- name: slo-burn-rate-@domio/data-service-avail-data
  rules:
      - alert: SLOBurnHighT2DataAvailData1h
        expr: |
          (
            sum(rate(http_requests_total{service="data",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="data"}[1h]))
          ) > 0.072000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/data-service-avail-data
          tier: tier-2
        annotations:
          summary: "@domio/data-service avail-data burning budget at >14.4× over 1h"
          runbook: "runbooks/data-service/availability.md"
      - alert: SLOBurnMedT2DataAvailData6h
        expr: |
          (
            sum(rate(http_requests_total{service="data",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="data"}[6h]))
          ) > 0.030000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/data-service-avail-data
          tier: tier-2
        annotations:
          summary: "@domio/data-service avail-data burning budget at >6× over 6h"
          runbook: "runbooks/data-service/availability.md"
      - alert: SLOBurnLowT2DataAvailData24h
        expr: |
          (
            sum(rate(http_requests_total{service="data",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="data"}[24h]))
          ) > 0.015000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/data-service-avail-data
          tier: tier-2
        annotations:
          summary: "@domio/data-service avail-data burning budget at >3× over 24h"
          runbook: "runbooks/data-service/availability.md"
      - alert: SLOBurnLowT2DataAvailData72h
        expr: |
          (
            sum(rate(http_requests_total{service="data",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="data"}[72h]))
          ) > 0.005000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/data-service-avail-data
          tier: tier-2
        annotations:
          summary: "@domio/data-service avail-data burning budget at >1× over 72h"
          runbook: "runbooks/data-service/availability.md"
- name: slo-burn-rate-@domio/connector-framework-avail-connector-framework
  rules:
      - alert: SLOBurnHighT2ConnectorFrameworkAvailConnectorFramework1h
        expr: |
          (
            sum(rate(http_requests_total{service="connector-framework",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="connector-framework"}[1h]))
          ) > 0.072000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/connector-framework-avail-connector-framework
          tier: tier-2
        annotations:
          summary: "@domio/connector-framework avail-connector-framework burning budget at >14.4× over 1h"
          runbook: "runbooks/connector-framework/availability.md"
      - alert: SLOBurnMedT2ConnectorFrameworkAvailConnectorFramework6h
        expr: |
          (
            sum(rate(http_requests_total{service="connector-framework",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="connector-framework"}[6h]))
          ) > 0.030000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/connector-framework-avail-connector-framework
          tier: tier-2
        annotations:
          summary: "@domio/connector-framework avail-connector-framework burning budget at >6× over 6h"
          runbook: "runbooks/connector-framework/availability.md"
      - alert: SLOBurnLowT2ConnectorFrameworkAvailConnectorFramework24h
        expr: |
          (
            sum(rate(http_requests_total{service="connector-framework",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="connector-framework"}[24h]))
          ) > 0.015000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/connector-framework-avail-connector-framework
          tier: tier-2
        annotations:
          summary: "@domio/connector-framework avail-connector-framework burning budget at >3× over 24h"
          runbook: "runbooks/connector-framework/availability.md"
      - alert: SLOBurnLowT2ConnectorFrameworkAvailConnectorFramework72h
        expr: |
          (
            sum(rate(http_requests_total{service="connector-framework",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="connector-framework"}[72h]))
          ) > 0.005000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/connector-framework-avail-connector-framework
          tier: tier-2
        annotations:
          summary: "@domio/connector-framework avail-connector-framework burning budget at >1× over 72h"
          runbook: "runbooks/connector-framework/availability.md"
- name: slo-burn-rate-@domio/query-gateway-lat-query-gateway-p95
  rules:
      - alert: SLOBurnHighT2QueryGatewayLatQueryGatewayP951h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="query-gateway",le="0.5"}[1h]))
              /
              sum(rate(http_request_duration_seconds_count{service="query-gateway"}[1h]))
            )
          ) > 0.720000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/query-gateway-lat-query-gateway-p95
          tier: tier-2
        annotations:
          summary: "@domio/query-gateway lat-query-gateway-p95 burning budget at >14.4× over 1h"
          runbook: "runbooks/query-gateway/latency.md"
      - alert: SLOBurnMedT2QueryGatewayLatQueryGatewayP956h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="query-gateway",le="0.5"}[6h]))
              /
              sum(rate(http_request_duration_seconds_count{service="query-gateway"}[6h]))
            )
          ) > 0.300000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/query-gateway-lat-query-gateway-p95
          tier: tier-2
        annotations:
          summary: "@domio/query-gateway lat-query-gateway-p95 burning budget at >6× over 6h"
          runbook: "runbooks/query-gateway/latency.md"
      - alert: SLOBurnLowT2QueryGatewayLatQueryGatewayP9524h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="query-gateway",le="0.5"}[24h]))
              /
              sum(rate(http_request_duration_seconds_count{service="query-gateway"}[24h]))
            )
          ) > 0.150000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/query-gateway-lat-query-gateway-p95
          tier: tier-2
        annotations:
          summary: "@domio/query-gateway lat-query-gateway-p95 burning budget at >3× over 24h"
          runbook: "runbooks/query-gateway/latency.md"
      - alert: SLOBurnLowT2QueryGatewayLatQueryGatewayP9572h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="query-gateway",le="0.5"}[72h]))
              /
              sum(rate(http_request_duration_seconds_count{service="query-gateway"}[72h]))
            )
          ) > 0.050000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/query-gateway-lat-query-gateway-p95
          tier: tier-2
        annotations:
          summary: "@domio/query-gateway lat-query-gateway-p95 burning budget at >1× over 72h"
          runbook: "runbooks/query-gateway/latency.md"
- name: slo-burn-rate-@domio/registry-service-avail-registry
  rules:
      - alert: SLOBurnHighT1RegistryAvailRegistry1h
        expr: |
          (
            sum(rate(http_requests_total{service="registry",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="registry"}[1h]))
          ) > 0.014400
        for: 2m
        labels:
          severity: page
          slo: @domio/registry-service-avail-registry
          tier: tier-1
        annotations:
          summary: "@domio/registry-service avail-registry burning budget at >14.4× over 1h"
          runbook: "runbooks/registry-service/availability.md"
      - alert: SLOBurnMedT1RegistryAvailRegistry6h
        expr: |
          (
            sum(rate(http_requests_total{service="registry",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="registry"}[6h]))
          ) > 0.006000
        for: 5m
        labels:
          severity: page
          slo: @domio/registry-service-avail-registry
          tier: tier-1
        annotations:
          summary: "@domio/registry-service avail-registry burning budget at >6× over 6h"
          runbook: "runbooks/registry-service/availability.md"
      - alert: SLOBurnLowT1RegistryAvailRegistry24h
        expr: |
          (
            sum(rate(http_requests_total{service="registry",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="registry"}[24h]))
          ) > 0.003000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/registry-service-avail-registry
          tier: tier-1
        annotations:
          summary: "@domio/registry-service avail-registry burning budget at >3× over 24h"
          runbook: "runbooks/registry-service/availability.md"
      - alert: SLOBurnLowT1RegistryAvailRegistry72h
        expr: |
          (
            sum(rate(http_requests_total{service="registry",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="registry"}[72h]))
          ) > 0.001000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/registry-service-avail-registry
          tier: tier-1
        annotations:
          summary: "@domio/registry-service avail-registry burning budget at >1× over 72h"
          runbook: "runbooks/registry-service/availability.md"
- name: slo-burn-rate-@domio/brand-service-avail-brand
  rules:
      - alert: SLOBurnHighT1BrandAvailBrand1h
        expr: |
          (
            sum(rate(http_requests_total{service="brand",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="brand"}[1h]))
          ) > 0.014400
        for: 2m
        labels:
          severity: page
          slo: @domio/brand-service-avail-brand
          tier: tier-1
        annotations:
          summary: "@domio/brand-service avail-brand burning budget at >14.4× over 1h"
          runbook: "runbooks/brand-service/availability.md"
      - alert: SLOBurnMedT1BrandAvailBrand6h
        expr: |
          (
            sum(rate(http_requests_total{service="brand",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="brand"}[6h]))
          ) > 0.006000
        for: 5m
        labels:
          severity: page
          slo: @domio/brand-service-avail-brand
          tier: tier-1
        annotations:
          summary: "@domio/brand-service avail-brand burning budget at >6× over 6h"
          runbook: "runbooks/brand-service/availability.md"
      - alert: SLOBurnLowT1BrandAvailBrand24h
        expr: |
          (
            sum(rate(http_requests_total{service="brand",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="brand"}[24h]))
          ) > 0.003000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/brand-service-avail-brand
          tier: tier-1
        annotations:
          summary: "@domio/brand-service avail-brand burning budget at >3× over 24h"
          runbook: "runbooks/brand-service/availability.md"
      - alert: SLOBurnLowT1BrandAvailBrand72h
        expr: |
          (
            sum(rate(http_requests_total{service="brand",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="brand"}[72h]))
          ) > 0.001000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/brand-service-avail-brand
          tier: tier-1
        annotations:
          summary: "@domio/brand-service avail-brand burning budget at >1× over 72h"
          runbook: "runbooks/brand-service/availability.md"
- name: slo-burn-rate-@domio/theme-service-avail-theme
  rules:
      - alert: SLOBurnHighT1ThemeAvailTheme1h
        expr: |
          (
            sum(rate(http_requests_total{service="theme",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="theme"}[1h]))
          ) > 0.014400
        for: 2m
        labels:
          severity: page
          slo: @domio/theme-service-avail-theme
          tier: tier-1
        annotations:
          summary: "@domio/theme-service avail-theme burning budget at >14.4× over 1h"
          runbook: "runbooks/theme-service/availability.md"
      - alert: SLOBurnMedT1ThemeAvailTheme6h
        expr: |
          (
            sum(rate(http_requests_total{service="theme",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="theme"}[6h]))
          ) > 0.006000
        for: 5m
        labels:
          severity: page
          slo: @domio/theme-service-avail-theme
          tier: tier-1
        annotations:
          summary: "@domio/theme-service avail-theme burning budget at >6× over 6h"
          runbook: "runbooks/theme-service/availability.md"
      - alert: SLOBurnLowT1ThemeAvailTheme24h
        expr: |
          (
            sum(rate(http_requests_total{service="theme",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="theme"}[24h]))
          ) > 0.003000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/theme-service-avail-theme
          tier: tier-1
        annotations:
          summary: "@domio/theme-service avail-theme burning budget at >3× over 24h"
          runbook: "runbooks/theme-service/availability.md"
      - alert: SLOBurnLowT1ThemeAvailTheme72h
        expr: |
          (
            sum(rate(http_requests_total{service="theme",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="theme"}[72h]))
          ) > 0.001000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/theme-service-avail-theme
          tier: tier-1
        annotations:
          summary: "@domio/theme-service avail-theme burning budget at >1× over 72h"
          runbook: "runbooks/theme-service/availability.md"
- name: slo-burn-rate-@domio/component-avail-component-registry
  rules:
      - alert: SLOBurnHighT1ComponentAvailComponentRegistry1h
        expr: |
          (
            sum(rate(http_requests_total{service="component",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="component"}[1h]))
          ) > 0.014400
        for: 2m
        labels:
          severity: page
          slo: @domio/component-avail-component-registry
          tier: tier-1
        annotations:
          summary: "@domio/component avail-component-registry burning budget at >14.4× over 1h"
          runbook: "runbooks/component/availability.md"
      - alert: SLOBurnMedT1ComponentAvailComponentRegistry6h
        expr: |
          (
            sum(rate(http_requests_total{service="component",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="component"}[6h]))
          ) > 0.006000
        for: 5m
        labels:
          severity: page
          slo: @domio/component-avail-component-registry
          tier: tier-1
        annotations:
          summary: "@domio/component avail-component-registry burning budget at >6× over 6h"
          runbook: "runbooks/component/availability.md"
      - alert: SLOBurnLowT1ComponentAvailComponentRegistry24h
        expr: |
          (
            sum(rate(http_requests_total{service="component",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="component"}[24h]))
          ) > 0.003000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/component-avail-component-registry
          tier: tier-1
        annotations:
          summary: "@domio/component avail-component-registry burning budget at >3× over 24h"
          runbook: "runbooks/component/availability.md"
      - alert: SLOBurnLowT1ComponentAvailComponentRegistry72h
        expr: |
          (
            sum(rate(http_requests_total{service="component",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="component"}[72h]))
          ) > 0.001000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/component-avail-component-registry
          tier: tier-1
        annotations:
          summary: "@domio/component avail-component-registry burning budget at >1× over 72h"
          runbook: "runbooks/component/availability.md"
- name: slo-burn-rate-@domio/font-service-lat-font-service-p95
  rules:
      - alert: SLOBurnHighT2FontLatFontServiceP951h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="font",le="0.2"}[1h]))
              /
              sum(rate(http_request_duration_seconds_count{service="font"}[1h]))
            )
          ) > 0.720000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/font-service-lat-font-service-p95
          tier: tier-2
        annotations:
          summary: "@domio/font-service lat-font-service-p95 burning budget at >14.4× over 1h"
          runbook: "runbooks/font-service/latency.md"
      - alert: SLOBurnMedT2FontLatFontServiceP956h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="font",le="0.2"}[6h]))
              /
              sum(rate(http_request_duration_seconds_count{service="font"}[6h]))
            )
          ) > 0.300000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/font-service-lat-font-service-p95
          tier: tier-2
        annotations:
          summary: "@domio/font-service lat-font-service-p95 burning budget at >6× over 6h"
          runbook: "runbooks/font-service/latency.md"
      - alert: SLOBurnLowT2FontLatFontServiceP9524h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="font",le="0.2"}[24h]))
              /
              sum(rate(http_request_duration_seconds_count{service="font"}[24h]))
            )
          ) > 0.150000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/font-service-lat-font-service-p95
          tier: tier-2
        annotations:
          summary: "@domio/font-service lat-font-service-p95 burning budget at >3× over 24h"
          runbook: "runbooks/font-service/latency.md"
      - alert: SLOBurnLowT2FontLatFontServiceP9572h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="font",le="0.2"}[72h]))
              /
              sum(rate(http_request_duration_seconds_count{service="font"}[72h]))
            )
          ) > 0.050000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/font-service-lat-font-service-p95
          tier: tier-2
        annotations:
          summary: "@domio/font-service lat-font-service-p95 burning budget at >1× over 72h"
          runbook: "runbooks/font-service/latency.md"
- name: slo-burn-rate-@domio/localization-avail-localization
  rules:
      - alert: SLOBurnHighT2LocalizationAvailLocalization1h
        expr: |
          (
            sum(rate(http_requests_total{service="localization",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="localization"}[1h]))
          ) > 0.072000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/localization-avail-localization
          tier: tier-2
        annotations:
          summary: "@domio/localization avail-localization burning budget at >14.4× over 1h"
          runbook: "runbooks/localization/availability.md"
      - alert: SLOBurnMedT2LocalizationAvailLocalization6h
        expr: |
          (
            sum(rate(http_requests_total{service="localization",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="localization"}[6h]))
          ) > 0.030000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/localization-avail-localization
          tier: tier-2
        annotations:
          summary: "@domio/localization avail-localization burning budget at >6× over 6h"
          runbook: "runbooks/localization/availability.md"
      - alert: SLOBurnLowT2LocalizationAvailLocalization24h
        expr: |
          (
            sum(rate(http_requests_total{service="localization",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="localization"}[24h]))
          ) > 0.015000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/localization-avail-localization
          tier: tier-2
        annotations:
          summary: "@domio/localization avail-localization burning budget at >3× over 24h"
          runbook: "runbooks/localization/availability.md"
      - alert: SLOBurnLowT2LocalizationAvailLocalization72h
        expr: |
          (
            sum(rate(http_requests_total{service="localization",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="localization"}[72h]))
          ) > 0.005000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/localization-avail-localization
          tier: tier-2
        annotations:
          summary: "@domio/localization avail-localization burning budget at >1× over 72h"
          runbook: "runbooks/localization/availability.md"
- name: slo-burn-rate-@domio/scenario-manager-avail-scenario-manager
  rules:
      - alert: SLOBurnHighT2ScenarioManagerAvailScenarioManager1h
        expr: |
          (
            sum(rate(http_requests_total{service="scenario-manager",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="scenario-manager"}[1h]))
          ) > 0.072000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/scenario-manager-avail-scenario-manager
          tier: tier-2
        annotations:
          summary: "@domio/scenario-manager avail-scenario-manager burning budget at >14.4× over 1h"
          runbook: "runbooks/scenario-manager/availability.md"
      - alert: SLOBurnMedT2ScenarioManagerAvailScenarioManager6h
        expr: |
          (
            sum(rate(http_requests_total{service="scenario-manager",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="scenario-manager"}[6h]))
          ) > 0.030000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/scenario-manager-avail-scenario-manager
          tier: tier-2
        annotations:
          summary: "@domio/scenario-manager avail-scenario-manager burning budget at >6× over 6h"
          runbook: "runbooks/scenario-manager/availability.md"
      - alert: SLOBurnLowT2ScenarioManagerAvailScenarioManager24h
        expr: |
          (
            sum(rate(http_requests_total{service="scenario-manager",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="scenario-manager"}[24h]))
          ) > 0.015000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/scenario-manager-avail-scenario-manager
          tier: tier-2
        annotations:
          summary: "@domio/scenario-manager avail-scenario-manager burning budget at >3× over 24h"
          runbook: "runbooks/scenario-manager/availability.md"
      - alert: SLOBurnLowT2ScenarioManagerAvailScenarioManager72h
        expr: |
          (
            sum(rate(http_requests_total{service="scenario-manager",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="scenario-manager"}[72h]))
          ) > 0.005000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/scenario-manager-avail-scenario-manager
          tier: tier-2
        annotations:
          summary: "@domio/scenario-manager avail-scenario-manager burning budget at >1× over 72h"
          runbook: "runbooks/scenario-manager/availability.md"
- name: slo-burn-rate-@domio/marketplace-service-avail-marketplace
  rules:
      - alert: SLOBurnHighT1MarketplaceAvailMarketplace1h
        expr: |
          (
            sum(rate(http_requests_total{service="marketplace",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="marketplace"}[1h]))
          ) > 0.014400
        for: 2m
        labels:
          severity: page
          slo: @domio/marketplace-service-avail-marketplace
          tier: tier-1
        annotations:
          summary: "@domio/marketplace-service avail-marketplace burning budget at >14.4× over 1h"
          runbook: "runbooks/marketplace-service/availability.md"
      - alert: SLOBurnMedT1MarketplaceAvailMarketplace6h
        expr: |
          (
            sum(rate(http_requests_total{service="marketplace",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="marketplace"}[6h]))
          ) > 0.006000
        for: 5m
        labels:
          severity: page
          slo: @domio/marketplace-service-avail-marketplace
          tier: tier-1
        annotations:
          summary: "@domio/marketplace-service avail-marketplace burning budget at >6× over 6h"
          runbook: "runbooks/marketplace-service/availability.md"
      - alert: SLOBurnLowT1MarketplaceAvailMarketplace24h
        expr: |
          (
            sum(rate(http_requests_total{service="marketplace",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="marketplace"}[24h]))
          ) > 0.003000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/marketplace-service-avail-marketplace
          tier: tier-1
        annotations:
          summary: "@domio/marketplace-service avail-marketplace burning budget at >3× over 24h"
          runbook: "runbooks/marketplace-service/availability.md"
      - alert: SLOBurnLowT1MarketplaceAvailMarketplace72h
        expr: |
          (
            sum(rate(http_requests_total{service="marketplace",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="marketplace"}[72h]))
          ) > 0.001000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/marketplace-service-avail-marketplace
          tier: tier-1
        annotations:
          summary: "@domio/marketplace-service avail-marketplace burning budget at >1× over 72h"
          runbook: "runbooks/marketplace-service/availability.md"
- name: slo-burn-rate-@domio/marketplace-preview-service-lat-marketplace-preview-p95
  rules:
      - alert: SLOBurnHighT2MarketplacePreviewLatMarketplacePreviewP951h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="marketplace-preview",le="1"}[1h]))
              /
              sum(rate(http_request_duration_seconds_count{service="marketplace-preview"}[1h]))
            )
          ) > 0.720000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/marketplace-preview-service-lat-marketplace-preview-p95
          tier: tier-2
        annotations:
          summary: "@domio/marketplace-preview-service lat-marketplace-preview-p95 burning budget at >14.4× over 1h"
          runbook: "runbooks/marketplace-preview-service/latency.md"
      - alert: SLOBurnMedT2MarketplacePreviewLatMarketplacePreviewP956h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="marketplace-preview",le="1"}[6h]))
              /
              sum(rate(http_request_duration_seconds_count{service="marketplace-preview"}[6h]))
            )
          ) > 0.300000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/marketplace-preview-service-lat-marketplace-preview-p95
          tier: tier-2
        annotations:
          summary: "@domio/marketplace-preview-service lat-marketplace-preview-p95 burning budget at >6× over 6h"
          runbook: "runbooks/marketplace-preview-service/latency.md"
      - alert: SLOBurnLowT2MarketplacePreviewLatMarketplacePreviewP9524h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="marketplace-preview",le="1"}[24h]))
              /
              sum(rate(http_request_duration_seconds_count{service="marketplace-preview"}[24h]))
            )
          ) > 0.150000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/marketplace-preview-service-lat-marketplace-preview-p95
          tier: tier-2
        annotations:
          summary: "@domio/marketplace-preview-service lat-marketplace-preview-p95 burning budget at >3× over 24h"
          runbook: "runbooks/marketplace-preview-service/latency.md"
      - alert: SLOBurnLowT2MarketplacePreviewLatMarketplacePreviewP9572h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="marketplace-preview",le="1"}[72h]))
              /
              sum(rate(http_request_duration_seconds_count{service="marketplace-preview"}[72h]))
            )
          ) > 0.050000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/marketplace-preview-service-lat-marketplace-preview-p95
          tier: tier-2
        annotations:
          summary: "@domio/marketplace-preview-service lat-marketplace-preview-p95 burning budget at >1× over 72h"
          runbook: "runbooks/marketplace-preview-service/latency.md"
- name: slo-burn-rate-@domio/deep-link-service-lat-deep-link-p95
  rules:
      - alert: SLOBurnHighT2DeepLinkLatDeepLinkP951h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="deep-link",le="0.2"}[1h]))
              /
              sum(rate(http_request_duration_seconds_count{service="deep-link"}[1h]))
            )
          ) > 0.720000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/deep-link-service-lat-deep-link-p95
          tier: tier-2
        annotations:
          summary: "@domio/deep-link-service lat-deep-link-p95 burning budget at >14.4× over 1h"
          runbook: "runbooks/deep-link-service/latency.md"
      - alert: SLOBurnMedT2DeepLinkLatDeepLinkP956h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="deep-link",le="0.2"}[6h]))
              /
              sum(rate(http_request_duration_seconds_count{service="deep-link"}[6h]))
            )
          ) > 0.300000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/deep-link-service-lat-deep-link-p95
          tier: tier-2
        annotations:
          summary: "@domio/deep-link-service lat-deep-link-p95 burning budget at >6× over 6h"
          runbook: "runbooks/deep-link-service/latency.md"
      - alert: SLOBurnLowT2DeepLinkLatDeepLinkP9524h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="deep-link",le="0.2"}[24h]))
              /
              sum(rate(http_request_duration_seconds_count{service="deep-link"}[24h]))
            )
          ) > 0.150000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/deep-link-service-lat-deep-link-p95
          tier: tier-2
        annotations:
          summary: "@domio/deep-link-service lat-deep-link-p95 burning budget at >3× over 24h"
          runbook: "runbooks/deep-link-service/latency.md"
      - alert: SLOBurnLowT2DeepLinkLatDeepLinkP9572h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="deep-link",le="0.2"}[72h]))
              /
              sum(rate(http_request_duration_seconds_count{service="deep-link"}[72h]))
            )
          ) > 0.050000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/deep-link-service-lat-deep-link-p95
          tier: tier-2
        annotations:
          summary: "@domio/deep-link-service lat-deep-link-p95 burning budget at >1× over 72h"
          runbook: "runbooks/deep-link-service/latency.md"
- name: slo-burn-rate-@domio/timeline-api-lat-timeline-api-p95
  rules:
      - alert: SLOBurnHighT2TimelineApiLatTimelineApiP951h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="timeline-api",le="0.15"}[1h]))
              /
              sum(rate(http_request_duration_seconds_count{service="timeline-api"}[1h]))
            )
          ) > 0.720000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/timeline-api-lat-timeline-api-p95
          tier: tier-2
        annotations:
          summary: "@domio/timeline-api lat-timeline-api-p95 burning budget at >14.4× over 1h"
          runbook: "runbooks/timeline-api/latency.md"
      - alert: SLOBurnMedT2TimelineApiLatTimelineApiP956h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="timeline-api",le="0.15"}[6h]))
              /
              sum(rate(http_request_duration_seconds_count{service="timeline-api"}[6h]))
            )
          ) > 0.300000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/timeline-api-lat-timeline-api-p95
          tier: tier-2
        annotations:
          summary: "@domio/timeline-api lat-timeline-api-p95 burning budget at >6× over 6h"
          runbook: "runbooks/timeline-api/latency.md"
      - alert: SLOBurnLowT2TimelineApiLatTimelineApiP9524h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="timeline-api",le="0.15"}[24h]))
              /
              sum(rate(http_request_duration_seconds_count{service="timeline-api"}[24h]))
            )
          ) > 0.150000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/timeline-api-lat-timeline-api-p95
          tier: tier-2
        annotations:
          summary: "@domio/timeline-api lat-timeline-api-p95 burning budget at >3× over 24h"
          runbook: "runbooks/timeline-api/latency.md"
      - alert: SLOBurnLowT2TimelineApiLatTimelineApiP9572h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="timeline-api",le="0.15"}[72h]))
              /
              sum(rate(http_request_duration_seconds_count{service="timeline-api"}[72h]))
            )
          ) > 0.050000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/timeline-api-lat-timeline-api-p95
          tier: tier-2
        annotations:
          summary: "@domio/timeline-api lat-timeline-api-p95 burning budget at >1× over 72h"
          runbook: "runbooks/timeline-api/latency.md"
- name: slo-burn-rate-@domio/calendar-service-avail-calendar
  rules:
      - alert: SLOBurnHighT2CalendarAvailCalendar1h
        expr: |
          (
            sum(rate(http_requests_total{service="calendar",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="calendar"}[1h]))
          ) > 0.072000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/calendar-service-avail-calendar
          tier: tier-2
        annotations:
          summary: "@domio/calendar-service avail-calendar burning budget at >14.4× over 1h"
          runbook: "runbooks/calendar-service/availability.md"
      - alert: SLOBurnMedT2CalendarAvailCalendar6h
        expr: |
          (
            sum(rate(http_requests_total{service="calendar",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="calendar"}[6h]))
          ) > 0.030000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/calendar-service-avail-calendar
          tier: tier-2
        annotations:
          summary: "@domio/calendar-service avail-calendar burning budget at >6× over 6h"
          runbook: "runbooks/calendar-service/availability.md"
      - alert: SLOBurnLowT2CalendarAvailCalendar24h
        expr: |
          (
            sum(rate(http_requests_total{service="calendar",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="calendar"}[24h]))
          ) > 0.015000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/calendar-service-avail-calendar
          tier: tier-2
        annotations:
          summary: "@domio/calendar-service avail-calendar burning budget at >3× over 24h"
          runbook: "runbooks/calendar-service/availability.md"
      - alert: SLOBurnLowT2CalendarAvailCalendar72h
        expr: |
          (
            sum(rate(http_requests_total{service="calendar",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="calendar"}[72h]))
          ) > 0.005000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/calendar-service-avail-calendar
          tier: tier-2
        annotations:
          summary: "@domio/calendar-service avail-calendar burning budget at >1× over 72h"
          runbook: "runbooks/calendar-service/availability.md"
- name: slo-burn-rate-@domio/meeting-integration-service-avail-meeting-integration
  rules:
      - alert: SLOBurnHighT2MeetingIntegrationAvailMeetingIntegration1h
        expr: |
          (
            sum(rate(http_requests_total{service="meeting-integration",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="meeting-integration"}[1h]))
          ) > 0.072000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/meeting-integration-service-avail-meeting-integration
          tier: tier-2
        annotations:
          summary: "@domio/meeting-integration-service avail-meeting-integration burning budget at >14.4× over 1h"
          runbook: "runbooks/meeting-integration-service/availability.md"
      - alert: SLOBurnMedT2MeetingIntegrationAvailMeetingIntegration6h
        expr: |
          (
            sum(rate(http_requests_total{service="meeting-integration",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="meeting-integration"}[6h]))
          ) > 0.030000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/meeting-integration-service-avail-meeting-integration
          tier: tier-2
        annotations:
          summary: "@domio/meeting-integration-service avail-meeting-integration burning budget at >6× over 6h"
          runbook: "runbooks/meeting-integration-service/availability.md"
      - alert: SLOBurnLowT2MeetingIntegrationAvailMeetingIntegration24h
        expr: |
          (
            sum(rate(http_requests_total{service="meeting-integration",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="meeting-integration"}[24h]))
          ) > 0.015000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/meeting-integration-service-avail-meeting-integration
          tier: tier-2
        annotations:
          summary: "@domio/meeting-integration-service avail-meeting-integration burning budget at >3× over 24h"
          runbook: "runbooks/meeting-integration-service/availability.md"
      - alert: SLOBurnLowT2MeetingIntegrationAvailMeetingIntegration72h
        expr: |
          (
            sum(rate(http_requests_total{service="meeting-integration",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="meeting-integration"}[72h]))
          ) > 0.005000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/meeting-integration-service-avail-meeting-integration
          tier: tier-2
        annotations:
          summary: "@domio/meeting-integration-service avail-meeting-integration burning budget at >1× over 72h"
          runbook: "runbooks/meeting-integration-service/availability.md"
- name: slo-burn-rate-@domio/crm-sync-avail-crm-sync
  rules:
      - alert: SLOBurnHighT3CrmSyncAvailCrmSync1h
        expr: |
          (
            sum(rate(http_requests_total{service="crm-sync",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="crm-sync"}[1h]))
          ) > 0.144000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/crm-sync-avail-crm-sync
          tier: tier-3
        annotations:
          summary: "@domio/crm-sync avail-crm-sync burning budget at >14.4× over 1h"
          runbook: "runbooks/crm-sync/availability.md"
      - alert: SLOBurnMedT3CrmSyncAvailCrmSync6h
        expr: |
          (
            sum(rate(http_requests_total{service="crm-sync",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="crm-sync"}[6h]))
          ) > 0.060000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/crm-sync-avail-crm-sync
          tier: tier-3
        annotations:
          summary: "@domio/crm-sync avail-crm-sync burning budget at >6× over 6h"
          runbook: "runbooks/crm-sync/availability.md"
      - alert: SLOBurnLowT3CrmSyncAvailCrmSync24h
        expr: |
          (
            sum(rate(http_requests_total{service="crm-sync",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="crm-sync"}[24h]))
          ) > 0.030000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/crm-sync-avail-crm-sync
          tier: tier-3
        annotations:
          summary: "@domio/crm-sync avail-crm-sync burning budget at >3× over 24h"
          runbook: "runbooks/crm-sync/availability.md"
      - alert: SLOBurnLowT3CrmSyncAvailCrmSync72h
        expr: |
          (
            sum(rate(http_requests_total{service="crm-sync",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="crm-sync"}[72h]))
          ) > 0.010000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/crm-sync-avail-crm-sync
          tier: tier-3
        annotations:
          summary: "@domio/crm-sync avail-crm-sync burning budget at >1× over 72h"
          runbook: "runbooks/crm-sync/availability.md"
- name: slo-burn-rate-@domio/analytics-warehouse-avail-analytics-warehouse
  rules:
      - alert: SLOBurnHighT3AnalyticsWarehouseAvailAnalyticsWarehouse1h
        expr: |
          (
            sum(rate(http_requests_total{service="analytics-warehouse",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="analytics-warehouse"}[1h]))
          ) > 0.144000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/analytics-warehouse-avail-analytics-warehouse
          tier: tier-3
        annotations:
          summary: "@domio/analytics-warehouse avail-analytics-warehouse burning budget at >14.4× over 1h"
          runbook: "runbooks/analytics-warehouse/availability.md"
      - alert: SLOBurnMedT3AnalyticsWarehouseAvailAnalyticsWarehouse6h
        expr: |
          (
            sum(rate(http_requests_total{service="analytics-warehouse",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="analytics-warehouse"}[6h]))
          ) > 0.060000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/analytics-warehouse-avail-analytics-warehouse
          tier: tier-3
        annotations:
          summary: "@domio/analytics-warehouse avail-analytics-warehouse burning budget at >6× over 6h"
          runbook: "runbooks/analytics-warehouse/availability.md"
      - alert: SLOBurnLowT3AnalyticsWarehouseAvailAnalyticsWarehouse24h
        expr: |
          (
            sum(rate(http_requests_total{service="analytics-warehouse",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="analytics-warehouse"}[24h]))
          ) > 0.030000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/analytics-warehouse-avail-analytics-warehouse
          tier: tier-3
        annotations:
          summary: "@domio/analytics-warehouse avail-analytics-warehouse burning budget at >3× over 24h"
          runbook: "runbooks/analytics-warehouse/availability.md"
      - alert: SLOBurnLowT3AnalyticsWarehouseAvailAnalyticsWarehouse72h
        expr: |
          (
            sum(rate(http_requests_total{service="analytics-warehouse",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="analytics-warehouse"}[72h]))
          ) > 0.010000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/analytics-warehouse-avail-analytics-warehouse
          tier: tier-3
        annotations:
          summary: "@domio/analytics-warehouse avail-analytics-warehouse burning budget at >1× over 72h"
          runbook: "runbooks/analytics-warehouse/availability.md"
- name: slo-burn-rate-@domio/ab-assignment-lat-ab-assignment-p95
  rules:
      - alert: SLOBurnHighT2AbAssignmentLatAbAssignmentP951h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="ab-assignment",le="0.05"}[1h]))
              /
              sum(rate(http_request_duration_seconds_count{service="ab-assignment"}[1h]))
            )
          ) > 0.720000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/ab-assignment-lat-ab-assignment-p95
          tier: tier-2
        annotations:
          summary: "@domio/ab-assignment lat-ab-assignment-p95 burning budget at >14.4× over 1h"
          runbook: "runbooks/ab-assignment/latency.md"
      - alert: SLOBurnMedT2AbAssignmentLatAbAssignmentP956h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="ab-assignment",le="0.05"}[6h]))
              /
              sum(rate(http_request_duration_seconds_count{service="ab-assignment"}[6h]))
            )
          ) > 0.300000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/ab-assignment-lat-ab-assignment-p95
          tier: tier-2
        annotations:
          summary: "@domio/ab-assignment lat-ab-assignment-p95 burning budget at >6× over 6h"
          runbook: "runbooks/ab-assignment/latency.md"
      - alert: SLOBurnLowT2AbAssignmentLatAbAssignmentP9524h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="ab-assignment",le="0.05"}[24h]))
              /
              sum(rate(http_request_duration_seconds_count{service="ab-assignment"}[24h]))
            )
          ) > 0.150000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/ab-assignment-lat-ab-assignment-p95
          tier: tier-2
        annotations:
          summary: "@domio/ab-assignment lat-ab-assignment-p95 burning budget at >3× over 24h"
          runbook: "runbooks/ab-assignment/latency.md"
      - alert: SLOBurnLowT2AbAssignmentLatAbAssignmentP9572h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="ab-assignment",le="0.05"}[72h]))
              /
              sum(rate(http_request_duration_seconds_count{service="ab-assignment"}[72h]))
            )
          ) > 0.050000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/ab-assignment-lat-ab-assignment-p95
          tier: tier-2
        annotations:
          summary: "@domio/ab-assignment lat-ab-assignment-p95 burning budget at >1× over 72h"
          runbook: "runbooks/ab-assignment/latency.md"
- name: slo-burn-rate-@domio/ab-measurement-avail-ab-measurement
  rules:
      - alert: SLOBurnHighT3AbMeasurementAvailAbMeasurement1h
        expr: |
          (
            sum(rate(http_requests_total{service="ab-measurement",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="ab-measurement"}[1h]))
          ) > 0.144000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/ab-measurement-avail-ab-measurement
          tier: tier-3
        annotations:
          summary: "@domio/ab-measurement avail-ab-measurement burning budget at >14.4× over 1h"
          runbook: "runbooks/ab-measurement/availability.md"
      - alert: SLOBurnMedT3AbMeasurementAvailAbMeasurement6h
        expr: |
          (
            sum(rate(http_requests_total{service="ab-measurement",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="ab-measurement"}[6h]))
          ) > 0.060000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/ab-measurement-avail-ab-measurement
          tier: tier-3
        annotations:
          summary: "@domio/ab-measurement avail-ab-measurement burning budget at >6× over 6h"
          runbook: "runbooks/ab-measurement/availability.md"
      - alert: SLOBurnLowT3AbMeasurementAvailAbMeasurement24h
        expr: |
          (
            sum(rate(http_requests_total{service="ab-measurement",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="ab-measurement"}[24h]))
          ) > 0.030000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/ab-measurement-avail-ab-measurement
          tier: tier-3
        annotations:
          summary: "@domio/ab-measurement avail-ab-measurement burning budget at >3× over 24h"
          runbook: "runbooks/ab-measurement/availability.md"
      - alert: SLOBurnLowT3AbMeasurementAvailAbMeasurement72h
        expr: |
          (
            sum(rate(http_requests_total{service="ab-measurement",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="ab-measurement"}[72h]))
          ) > 0.010000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/ab-measurement-avail-ab-measurement
          tier: tier-3
        annotations:
          summary: "@domio/ab-measurement avail-ab-measurement burning budget at >1× over 72h"
          runbook: "runbooks/ab-measurement/availability.md"
- name: slo-burn-rate-@domio/ab-statistics-avail-ab-statistics
  rules:
      - alert: SLOBurnHighT3AbStatisticsAvailAbStatistics1h
        expr: |
          (
            sum(rate(http_requests_total{service="ab-statistics",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="ab-statistics"}[1h]))
          ) > 0.144000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/ab-statistics-avail-ab-statistics
          tier: tier-3
        annotations:
          summary: "@domio/ab-statistics avail-ab-statistics burning budget at >14.4× over 1h"
          runbook: "runbooks/ab-statistics/availability.md"
      - alert: SLOBurnMedT3AbStatisticsAvailAbStatistics6h
        expr: |
          (
            sum(rate(http_requests_total{service="ab-statistics",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="ab-statistics"}[6h]))
          ) > 0.060000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/ab-statistics-avail-ab-statistics
          tier: tier-3
        annotations:
          summary: "@domio/ab-statistics avail-ab-statistics burning budget at >6× over 6h"
          runbook: "runbooks/ab-statistics/availability.md"
      - alert: SLOBurnLowT3AbStatisticsAvailAbStatistics24h
        expr: |
          (
            sum(rate(http_requests_total{service="ab-statistics",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="ab-statistics"}[24h]))
          ) > 0.030000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/ab-statistics-avail-ab-statistics
          tier: tier-3
        annotations:
          summary: "@domio/ab-statistics avail-ab-statistics burning budget at >3× over 24h"
          runbook: "runbooks/ab-statistics/availability.md"
      - alert: SLOBurnLowT3AbStatisticsAvailAbStatistics72h
        expr: |
          (
            sum(rate(http_requests_total{service="ab-statistics",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="ab-statistics"}[72h]))
          ) > 0.010000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/ab-statistics-avail-ab-statistics
          tier: tier-3
        annotations:
          summary: "@domio/ab-statistics avail-ab-statistics burning budget at >1× over 72h"
          runbook: "runbooks/ab-statistics/availability.md"
- name: slo-burn-rate-@domio/benchmark-avail-benchmark
  rules:
      - alert: SLOBurnHighT3BenchmarkAvailBenchmark1h
        expr: |
          (
            sum(rate(http_requests_total{service="benchmark",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="benchmark"}[1h]))
          ) > 0.144000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/benchmark-avail-benchmark
          tier: tier-3
        annotations:
          summary: "@domio/benchmark avail-benchmark burning budget at >14.4× over 1h"
          runbook: "runbooks/benchmark/availability.md"
      - alert: SLOBurnMedT3BenchmarkAvailBenchmark6h
        expr: |
          (
            sum(rate(http_requests_total{service="benchmark",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="benchmark"}[6h]))
          ) > 0.060000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/benchmark-avail-benchmark
          tier: tier-3
        annotations:
          summary: "@domio/benchmark avail-benchmark burning budget at >6× over 6h"
          runbook: "runbooks/benchmark/availability.md"
      - alert: SLOBurnLowT3BenchmarkAvailBenchmark24h
        expr: |
          (
            sum(rate(http_requests_total{service="benchmark",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="benchmark"}[24h]))
          ) > 0.030000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/benchmark-avail-benchmark
          tier: tier-3
        annotations:
          summary: "@domio/benchmark avail-benchmark burning budget at >3× over 24h"
          runbook: "runbooks/benchmark/availability.md"
      - alert: SLOBurnLowT3BenchmarkAvailBenchmark72h
        expr: |
          (
            sum(rate(http_requests_total{service="benchmark",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="benchmark"}[72h]))
          ) > 0.010000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/benchmark-avail-benchmark
          tier: tier-3
        annotations:
          summary: "@domio/benchmark avail-benchmark burning budget at >1× over 72h"
          runbook: "runbooks/benchmark/availability.md"
- name: slo-burn-rate-@domio/lint-service-lat-lint-p95
  rules:
      - alert: SLOBurnHighT2LintLatLintP951h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="lint",le="1"}[1h]))
              /
              sum(rate(http_request_duration_seconds_count{service="lint"}[1h]))
            )
          ) > 0.720000
        for: 2m
        labels:
          severity: ticket
          slo: @domio/lint-service-lat-lint-p95
          tier: tier-2
        annotations:
          summary: "@domio/lint-service lat-lint-p95 burning budget at >14.4× over 1h"
          runbook: "runbooks/lint-service/latency.md"
      - alert: SLOBurnMedT2LintLatLintP956h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="lint",le="1"}[6h]))
              /
              sum(rate(http_request_duration_seconds_count{service="lint"}[6h]))
            )
          ) > 0.300000
        for: 5m
        labels:
          severity: ticket
          slo: @domio/lint-service-lat-lint-p95
          tier: tier-2
        annotations:
          summary: "@domio/lint-service lat-lint-p95 burning budget at >6× over 6h"
          runbook: "runbooks/lint-service/latency.md"
      - alert: SLOBurnLowT2LintLatLintP9524h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="lint",le="1"}[24h]))
              /
              sum(rate(http_request_duration_seconds_count{service="lint"}[24h]))
            )
          ) > 0.150000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/lint-service-lat-lint-p95
          tier: tier-2
        annotations:
          summary: "@domio/lint-service lat-lint-p95 burning budget at >3× over 24h"
          runbook: "runbooks/lint-service/latency.md"
      - alert: SLOBurnLowT2LintLatLintP9572h
        expr: |
          (
            1 - (
              sum(rate(http_request_duration_seconds_bucket{service="lint",le="1"}[72h]))
              /
              sum(rate(http_request_duration_seconds_count{service="lint"}[72h]))
            )
          ) > 0.050000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/lint-service-lat-lint-p95
          tier: tier-2
        annotations:
          summary: "@domio/lint-service lat-lint-p95 burning budget at >1× over 72h"
          runbook: "runbooks/lint-service/latency.md"
- name: slo-burn-rate-@domio/viewer-identity-avail-viewer-identity
  rules:
      - alert: SLOBurnHighT1ViewerIdentityAvailViewerIdentity1h
        expr: |
          (
            sum(rate(http_requests_total{service="viewer-identity",status=~"5.."}[1h]))
            /
            sum(rate(http_requests_total{service="viewer-identity"}[1h]))
          ) > 0.014400
        for: 2m
        labels:
          severity: page
          slo: @domio/viewer-identity-avail-viewer-identity
          tier: tier-1
        annotations:
          summary: "@domio/viewer-identity avail-viewer-identity burning budget at >14.4× over 1h"
          runbook: "runbooks/viewer-identity/availability.md"
      - alert: SLOBurnMedT1ViewerIdentityAvailViewerIdentity6h
        expr: |
          (
            sum(rate(http_requests_total{service="viewer-identity",status=~"5.."}[6h]))
            /
            sum(rate(http_requests_total{service="viewer-identity"}[6h]))
          ) > 0.006000
        for: 5m
        labels:
          severity: page
          slo: @domio/viewer-identity-avail-viewer-identity
          tier: tier-1
        annotations:
          summary: "@domio/viewer-identity avail-viewer-identity burning budget at >6× over 6h"
          runbook: "runbooks/viewer-identity/availability.md"
      - alert: SLOBurnLowT1ViewerIdentityAvailViewerIdentity24h
        expr: |
          (
            sum(rate(http_requests_total{service="viewer-identity",status=~"5.."}[24h]))
            /
            sum(rate(http_requests_total{service="viewer-identity"}[24h]))
          ) > 0.003000
        for: 10m
        labels:
          severity: ticket
          slo: @domio/viewer-identity-avail-viewer-identity
          tier: tier-1
        annotations:
          summary: "@domio/viewer-identity avail-viewer-identity burning budget at >3× over 24h"
          runbook: "runbooks/viewer-identity/availability.md"
      - alert: SLOBurnLowT1ViewerIdentityAvailViewerIdentity72h
        expr: |
          (
            sum(rate(http_requests_total{service="viewer-identity",status=~"5.."}[72h]))
            /
            sum(rate(http_requests_total{service="viewer-identity"}[72h]))
          ) > 0.001000
        for: 30m
        labels:
          severity: ticket
          slo: @domio/viewer-identity-avail-viewer-identity
          tier: tier-1
        annotations:
          summary: "@domio/viewer-identity avail-viewer-identity burning budget at >1× over 72h"
          runbook: "runbooks/viewer-identity/availability.md"
