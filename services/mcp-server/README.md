# @domio/mcp-server-service

> ⚠️ **This is the Phase 0 TypeScript MCP stub.** It is retained for
> backward compatibility only. The production MCP server is the Go
> service at `services/mcp-server/cmd/mcp-server/` (Phase 13 M1).
>
> **Source of truth:** `docs/mcp-server.md` and the Go package at
> `internal/{audit,gateway,registry,store,tools}`.

## Owner

Phase 13 — M1 owned by P13 workstream (assigned at kickoff).

## Public API surface

See `contracts/mcp/README.md` for JSON-RPC 2.0 wire contract and
the six M1 tool specs in `contracts/mcp/tools/*.schema.json`.
