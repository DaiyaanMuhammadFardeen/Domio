{
  "name": "@domio/participant-session",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Phase 16 W1 — Participant session service. Mirrors presenter-session; consumed by audience-session-store.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --max-warnings 0",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist .turbo coverage"
  },
  "dependencies": {
    "@domio/audience-service": "workspace:^",
    "@domio/audit-ts": "workspace:*",
    "@domio/edge-pubsub": "workspace:*",
    "@domio/session-code": "workspace:*",
    "@domio/text-normalize": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "22.10.1",
    "@types/pg": "^8.20.3",
    "pg": "^8.22.0",
    "typescript": "5.7.2",
    "vitest": "2.1.8"
  }
}