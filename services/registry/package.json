{
  "name": "@domio/registry-service",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Component / theme / marketplace registry. Phase 0 stub; Phase 06.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src --max-warnings 0",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist .turbo coverage"
  },
  "devDependencies": {
    "@types/pg": "^8.20.3",
    "typescript": "5.7.2",
    "vitest": "2.1.8"
  },
  "dependencies": {
    "hono": "4.6.13",
    "pg": "^8.22.0",
    "@domio/schema-prop": "workspace:*",
    "@domio/schema": "workspace:*",
    "@domio/components": "workspace:*"
  }
}